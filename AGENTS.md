# STIZ 프로젝트 작업 규칙

이 파일은 이 저장소를 여는 모든 Codex 작업자가 가장 먼저 읽는 공통 운영 규칙이다.

## 사용자와 작업 방식

- 사용자는 비개발자에 가까운 바이브 코더다. 한국어로 설명하고, 전문 용어에는 쉬운 비유나 예시를 붙인다.
- 이유를 방법보다 먼저 설명한다.
- 코드를 수정하거나 파일을 변경하기 전에는 변경 목적과 묶음 범위를 설명하고 `이렇게 수정해도 될까요?`라고 승인을 받는다.
- 명확한 운영 요청은 진행하되, 운영 DB 쓰기·배포·삭제·청구·결제·환불·문자·알림·초대 발송은 실행 직전 대상과 영향을 다시 보여주고 승인받는다.
- 청구나 알림을 준비하라는 요청은 실제 발행·발송 승인으로 간주하지 않는다.

## 시작할 때 읽을 문서

1. `.Codex/HANDOFF.md`: 현재 운영 실행기, 승인 대기, 재개 지점
2. `.Codex/scratchpad.md`: 현재 개발 작업과 검증 상태
3. `.Codex/knowledge/index.md`: 장기 지식과 관련 문서 색인
4. STIZ 운영·청구 작업이면 `.agents/skills/stiz-monthly-billing/SKILL.md`
5. 새 PC 설치나 실행기 교대면 `docs/codex-handoff-setup.md`

## 작업 그룹과 검증

- 전체 요청을 결과물과 의존관계 기준으로 그룹화한다.
- 서로 파일이 겹치지 않는 조사·UI·API·데이터·테스트는 병렬로 진행한다.
- 같은 파일을 두 작업자가 동시에 수정하지 않는다.
- 개발 후 tester 검증을 통과해야 PM이 커밋한다. 소규모 변경은 `tsc --noEmit` 통과로 대신할 수 있다.
- 기존 사용자 변경과 미추적 `_codex_locked_old/`는 건드리지 않는다.

## 다중 에이전트 협업

- 이 저장소의 루트 `AGENTS.md`는 홈페이지 기능 개발 세션 한 곳만 편집한다. 다른 학원 세션은 규칙을 읽고 업무에 적용하되 같은 파일을 동시에 수정하지 않는다.
- 파일뿐 아니라 운영 실행기와 기기도 작업 시작 전에 단일 소유자를 정한다. 같은 파일, 같은 운영 반영, 같은 예약 작업을 두 세션이나 두 기기가 동시에 실행하지 않는다.
- 내부 에이전트나 다른 작업자에게 질문을 보낸 뒤에는 답변만 기다리지 말고, 파일과 운영 대상이 겹치지 않는 독립 작업을 계속한다. 답변이 도착하면 보류했던 후속 작업을 다시 이어간다.
- 내부 협의에서 질문, 이견, 누락이 남아 있으면 요청자에게 중계를 맡기지 말고 담당자끼리 수정과 재검증을 이어간다.
- 사용자에게는 로그인, 인증 코드, 물리 기기 조작, 정책 선택처럼 실제 사용자만 해결할 수 있는 구체적인 장애가 있을 때만 판단을 요청한다. 단순한 진행 상황이나 내부 질의 대기는 장애로 올리지 않는다.
- 단, 운영 쓰기·발송·배포 등 기존 실행 직전 승인 요청은 장애 상승과 별개로 반드시 수행한다.
- 완료 보고에는 변경 경로, 검증 명령과 결과, 남은 위험 또는 보류 사항을 증거로 남긴다. 요청을 보낸 세션에도 같은 결과를 직접 회신하고, 그 회신을 해당 작업에서 합의한 정본 기록에 반영한다.
- 전문 담당자 사이의 개별 수신 확인, 테스트, 수정, 재검증 왕복은 원 작업에서 관리하고 총괄에 매번 참조 발송하지 않는다. 주 담당이 수정과 독립 검증의 완료 여부를 끝까지 추적한다.
- 총괄에는 그룹 완료, 해결하지 못한 의존 관계나 소유권 충돌, 실제 사용자 결정이 필요한 사항만 `작업 키 / 결과와 잔여 / 대상 버전·상태 / 독립 검증과 증거 위치 / 미결 / 다음 담당과 행동 / 검증된 지식 변화`로 압축해 한 번에 보고한다. 중대 위험은 작업을 즉시 멈추고 바로 보고한다.

## 승인 범위의 재사용

- 사용자가 이미 승인한 대상, 값, 범위, 실행 시점이 모두 그대로라면 같은 작업을 다시 승인해 달라고 묻지 않는다.
- 대상, 금액, 기간, 수신자, 문구, 실행 환경 또는 영향이 달라지면 기존 승인을 재사용하지 않고 변경된 내용을 보여준 뒤 다시 승인받는다.
- 기존 DB 쓰기, 외부 발송, 배포, 결제, 삭제, 개인정보 및 플랫폼 안전 통제는 그대로 유지한다. 이 규칙은 새 운영 권한을 부여하거나 기존 승인 범위를 일괄 축소하지 않는다.

## Git 규칙

- 커밋 메시지는 `feat:`, `fix:`, `style:`, `refactor:`, `docs:`, `test:` 형식이며 설명은 한국어로 쓴다.
- 모든 커밋에 다음 공동 작성자를 포함한다.
  `Co-Authored-By: Codex Opus 4.6 (1M context) <noreply@anthropic.com>`
- tester 통과 후 로컬 커밋은 자동으로 할 수 있다.
- 푸시는 사용자가 `푸시해` 또는 `push`라고 요청한 경우에만 한다.
- 완료 보고에는 미푸시 커밋 수를 포함한다.

## STIZ 운영 불변 조건

- Google Sheets, Rallyz, `stiz-dasan.kr`를 하나의 연결된 업무로 취급한다.
- 이름만으로 학생을 확정하지 않는다. 가능한 경우 학생 ID, 생년월일, 보호자 연락처 끝자리, 반, 적용 월을 함께 사용한다.
- 외부 변경 전 dry-run 원장을 만들고, 모호한 항목은 추측하지 않고 `HELD` 또는 확인보류로 남긴다.
- 승인된 변경은 시트 → Rallyz → 사이트 순으로 반영하고 세 시스템을 다시 읽어 검증한다.
- 휴원·퇴원·이월·0원 청구서는 정규 청구에서 제외한다.
- 청구서 발행 알림은 청구 발행 승인에 포함된 명시적 대상으로 취급하며, 수신자·금액·기간이 바뀌면 다시 승인받는다.
- 예약 대조와 실제 외부 쓰기를 수행하는 운영 실행기는 한 대만 `ACTIVE`로 둔다. 두 PC에 같은 예약을 동시에 켜지 않는다.

## 보안

- 비밀번호, API 키, 서비스 계정 private key/JSON, DB URL, 쿠키, 세션, 초대 토큰, 전체 전화번호·주소·생년월일을 Git·문서·Issue·로그에 기록하지 않는다.
- `.env.local`과 인증 파일은 각 PC의 안전한 로컬 저장소에 두고 Git에 올리지 않는다.
- 개인정보가 필요한 운영 기록은 안정 ID와 마스킹 값을 사용한다.

## 개발서버

- `taskkill //f //im node.exe`는 절대 사용하지 않는다.
- 재시작이 필요하면 `netstat -ano | findstr :<포트>`로 PID를 찾고 그 PID만 종료한다.

## 기록 책임

- 작업 완료 후 `.Codex/scratchpad.md` 작업 로그와 진행 현황을 갱신한다.
- 기술 결정은 `decisions.md`, 반복 규칙은 `conventions.md`, 오류 해결은 `errors.md`, 장기 교훈은 `lessons.md`에 기록하고 `index.md`를 갱신한다.
- scratchpad 작업 로그는 최근 10건만 유지하고 전체를 100줄 이내로 정리한다.
