# STIZ Knowledge Index

> 2026-07-22 갱신: 체험신청 목록 테이블은 PC에서 `lg:min-w-0`로 부모 폭에 맞추고, 모바일에서만 최소 폭으로 가로 스크롤을 허용한다. 번개 퀵액션 메뉴는 테이블 스크롤 박스 안에 갇히지 않도록 화면 기준 `fixed` 플로팅 메뉴로 띄운다.

> 2026-07-22 갱신: 체험신청 목록 컬럼은 `상태/신청일/체험일/수업/수강생이름/학교/학년/상태변경/액션`만 유지한다. 쌤알림은 독립 컬럼이 아니라 번개 퀵액션 메뉴 안에서 발송/재발송한다.

> 2026-07-22 갱신: 체험신청 목록은 신청일/체험일을 상태보다 왼쪽에 배치하고, 기본 정렬은 체험일 오름차순으로 둔다. 체험일 빠른 필터는 전체, 오늘 이후, 오늘, 이번 주, 지난 일정, 미입력으로 운영한다.

> 2026-07-22 갱신: 체험신청 목록은 신청일/체험일을 날짜만 표시하고, 수업교시와 쌤알림을 별도 컬럼으로 분리한다. 쌤알림은 목록에서 발송, 완료일 확인, 재발송까지 바로 처리한다.

> 2026-07-22 갱신: 셔틀 확정 노선은 기사 체크가 모두 끝난 뒤 `COMPLETED` 상태로 운행 완료 기록을 남긴다. 관리자 셔틀 화면은 30초 자동 새로고침과 수동 새 상태 불러오기로 기사 앱 체크 현황을 빠르게 반영한다.

> 2026-07-22 갱신: 체험/수강신청 관리는 카드형 없이 진짜 한 줄 목록형만 유지한다. 보호자 컬럼은 두지 않고 학생/연락처 칸에 보호자 이름과 전화번호를 함께 압축하며, 우측 액션은 단일 버튼을 눌렀을 때 플로팅 메뉴로 펼친다.

> 2026-07-21 갱신: 셔틀 노선은 `driverUserId`로 `DRIVER` 역할 사용자에게 배정하며, 확정 전 차량·기사·탑승 학생을 모두 요구한다. 기사 모바일 `/staff/shuttle`은 로그인한 기사 본인에게 배정된 확정 노선만 보여준다.

> 2026-07-21 갱신: 체험신청서에 이미 입력된 연락처는 수강신청서에서 다시 쓰게 하지 않는다. `childPhone`이 없으면 보호자 연락처를 수강생 연락처 기본값으로 사용하고, `미입력` 보호자 이름은 빈칸으로 보여준다.

> 2026-07-21 갱신: 스태프 역할에 `DRIVER`를 추가하고 기사 계정은 로그인 후 `/staff/shuttle`로 이동한다. 초대/가입은 코치/강사와 셔틀 기사 모두 개인 초대 링크를 사용하며, 실제 노선 배정·탑승 체크는 다음 단계에서 셔틀 관리 데이터와 연결한다.

> 2026-07-21 갱신: 체험완료 리드는 직접 등록보다 수강신청 안내 발송을 먼저 노출하고, `/apply/enroll?trialId=...`는 학교·농구 경험·희망 수업까지 자동 채움한다.

> 2026-07-21 갱신: 방학특강 셔틀 노선은 공개 안내용 셔틀 정보와 분리된 실제 운행 계획 모델로 관리하며, 확정 노선은 보관/수정본 생성으로 이력을 남긴다.

> 2026-07-21 갱신: 방학특강 승인 항목은 관리자 목록에서 여러 건을 선택해 수강 등록과 청구서 생성을 묶음 처리할 수 있으며, 실패 항목은 선택 상태로 남겨 재처리한다.

> 2026-07-21 갱신: 방학특강 셔틀 지도 핀은 학부모 제출과 관리자 검증을 구분하고, 서버 고정 동의 버전·좌표 검증·텍스트 fallback을 적용한다.

> 2026-07-21 갱신: 방학특강 일괄 처리는 학생 행 선택 UI를 제공하되 실제 상태 변경은 반별 `SpecialProgramApplicationItem` 단위로 처리한다.

> 2026-07-21 갱신: 신청 관리의 목록형은 div grid 카드가 아니라 실제 `table/thead/tbody` 기반 스프레드시트형 테이블로 유지한다.

> 2026-07-21 갱신: 관리자 체험신청 카드형은 다섯 열 표 구조를 쓰지 않고, 상단 핵심 정보·일정 타일·하단 액션 구조로 배치해 PC 폭에서도 배지가 세로로 깨지지 않게 한다.

> 2026-07-21 갱신: 체험신청 완료 화면은 체험비 입금 안내, 계좌번호 복사, 송금 정보 공유를 즉시 제공하며 약관과 같은 체험비 계좌 상수를 사용한다.

> 2026-07-21 갱신: 체험/수강신청 취소는 삭제하지 않고 `CANCELLED` 상태로 보존하며, 승인된 수강신청 변경은 원생/수강 등록 메뉴에서 처리한다.

> 2026-07-20 갱신: 계절특강 초안 파일이 `SeasonalApplyClient` 없이 추가되면 전체 `tsc --noEmit`이 실패하므로 결제 작업과 분리해 검증한다.

> 2026-07-16 갱신: 공개 홈페이지 계정 진입과 마이페이지 권한은 토큰 metadata가 아닌 DB 역할을 기준으로 판정한다.

> 2026-07-16 갱신: 수강생 시트의 랠리즈·카드·현금영수증은 수납 완료, 미납/미결제만 미납, 이월은 취소/이월 청구로 처리한다.

> 2026-07-16 갱신: 로그인은 DB 역할별 업무 홈으로 이동하고, 교사용 앱과 공개 홈페이지는 프로필 메뉴와 공개 헤더를 통해 양방향으로 연결한다.

> 2026-07-16 갱신: 관리자 신청/체험 긴 목록은 첫 진입 payload를 줄이고 `limit/offset` 기반 서버 페이지 로딩으로 처리한다.

> 2026-07-16 갱신: 관리자 신청/체험 상담 기록은 `ApplicationContactLog`에 누적하고, 목록에는 최신 연락 1건과 열린 재연락 1건만 결합해 속도를 유지한다.

> 2026-07-16 갱신: 교사용 앱은 한 교사당 하나의 수업만 진행하며, 수업 기록은 자동 저장과 네트워크 실패 복구를 기본 운영 원칙으로 적용한다.

> 2026-07-16 갱신: 관리자 신청/체험 목록은 전화, 연락처 복사, 상담용 요약 복사를 바로 제공해 연락 업무를 목록 안에서 끝낼 수 있게 한다.

> 2026-07-16 갱신: 교사 가입을 관리자 개인 초대 링크로 제한하고 SMS 실패와 가입 링크 생성을 분리하는 운영 원칙을 기록했다.

> 2026-07-16 갱신: 관리자 신청/체험 목록은 24시간 이상 대기, 중복 연락처, 후속 안내 필요를 자동으로 강조해 먼저 처리할 대상을 드러낸다.

> 2026-07-16 갱신: 관리자 신청/체험 목록은 검색·운영 빠른 필터·화면 안 처리 피드백을 기본 운영 UX로 둔다.

> 2026-07-15 갱신: 공용 교사용 앱 설치 링크와 개인 초대 토큰 분리, 기기별 PWA 설치, 재설치 상태 판단 원칙을 기록했다.

> 2026-07-15 갱신: 관리자 신청/체험 화면은 내부 키와 개발 문구를 숨기고, 운영자가 바로 처리할 수 있는 요약·배지·쉬운 오류 문구를 우선한다.

> 2026-07-15 갱신: 수업 종료 저장 뒤 학부모 알림 실패가 화면 무반응으로 보이던 오류와 상태 변경·부가 알림 분리 원칙을 기록했다.

> 2026-07-15 갱신: 교사용 앱의 공식 진입점(`/staff`), 전용 로그인, 독립 PWA 범위, 개인정보 비캐시, 카메라·마이크 최소 권한 결정을 기록했다.

> 2026-07-15 갱신: 교사용 앱은 현재·다음 수업을 최우선으로 표시하며, 수업 진행 중에는 하단 메뉴를 숨기고 타이머·출석·사진·음성 메모·종료 행동을 한 손 조작 순서로 배치한다.

> 2026-07-15 갱신: 미디어 회수는 동의·초안 원본이 삭제돼도 불변 스냅샷의 철회 증거와 공개 Storage 경로로 계속 처리하며, 배포 전 `release:preflight`와 별도 전체 DB 복구 확인을 필수로 한다.

> 2026-07-15 갱신: 교사용 앱 운영 보안·푸시·미디어 회수·결제 귀속 스키마 적용 완료. 결제의 classId는 삭제를 막는 FK가 아닌 역사 스냅샷으로 보존하고, 외부 미디어 회수 작업은 원본 삭제 후에도 감사 스냅샷을 유지한다.

## 기준
- 기준일: 2026-07-21
- 문서 수: 5
- 프로젝트: STIZ 농구교실 다산점 홈페이지 및 관리자 시스템

## 최근 지식
- 방학특강 셔틀 노선은 `ShuttleVehicle`, `ShuttleRoutePlan`, `ShuttleRouteStop`, `ShuttleRoutePassenger`, `ShuttleAuditLog`로 관리한다. 확정된 노선은 직접 수정하지 않고 새 수정본을 만들어 실제 운행 기준과 변경 이력을 분리한다.
- 방학특강 수강·청구 전환은 학생 신청서 전체가 아니라 승인된 신청 반 항목 단위로 처리한다. 묶음 처리 중 일부가 실패하면 성공 건은 유지하고 실패한 항목만 다시 선택해 운영자가 이어서 처리한다.
- 방학특강 셔틀 위치는 주소와 좌표를 함께 저장하며 카카오 지도 장애 시 기존 텍스트 신청을 유지한다. 학부모 제출 시각은 관리자 검증 완료를 의미하지 않는다.
- 방학특강 신청 목록의 복수 처리는 현재 검색/필터 결과에 보이는 학생만 전체 선택 대상으로 삼고, 실제 상태 변경은 신청 반 항목 단위로 처리한다. 정원 확인과 대기순번 계산은 단건 처리와 같은 서버 함수를 사용한다.
- 신청 관리의 체험/수강신청 화면은 카드형을 제공하지 않는다. 목록형은 실제 `<table>`, `<thead>`, `<tbody>`를 사용하고, 보호자 컬럼 없이 학생/연락처 한 줄 요약과 우측 단일 액션 버튼의 플로팅 메뉴로 정보 밀도와 조작성을 유지한다.
- 체험신청 목록의 신청일/체험일은 시간 없이 날짜만 표시한다. 쌤알림 상태는 수업교시 텍스트에 섞지 않고 별도 컬럼에서 발송/완료/재발송으로 관리한다.
- 체험신청 기본 목록은 체험일 오름차순으로 정렬한다. 체험일이 없는 건은 아래로 보내고, 날짜 필터로 오늘 이후/오늘/이번 주/지난 일정/미입력 건을 좁혀 본다.
- 체험신청 행의 상태 변경은 표 안의 `상태변경` 컬럼에서 처리한다. `체험완료`가 되면 수강신청 안내 버튼을 같은 셀에 바로 노출하고, 기타 빠른 작업은 우측 번개 아이콘 메뉴에 둔다.
- 관리자 체험신청 카드형은 `xl:grid-cols-[...]` 같은 고정 다열 표 구조를 피한다. 상태/유입/우선처리 배지는 `whitespace-nowrap`로 한 덩어리 칩처럼 유지하고, 일정 정보는 `auto-fit` 타일 grid로 카드 폭에 맞춰 재배치한다.
- 체험신청 완료 화면은 학부모가 바로 납부 행동을 할 수 있게 체험비 금액, 카카오뱅크 계좌, 예금주, 계좌번호 복사, 송금 정보 공유를 함께 보여준다. 공개 송금 딥링크는 앱 정책 영향을 받을 수 있으므로 복사/공유 fallback을 기본으로 둔다.
- 체험/수강신청 취소는 운영 이력이므로 삭제하지 않고 `CANCELLED` 상태와 사유로 보존한다. 승인된 수강신청은 이미 원생/수강 데이터로 전환되었으므로 신청서 수정이 아니라 운영 원장 메뉴에서 변경한다.
- 원생 관리 최신 원생목록 점검은 시트 행 수와 실제 학생 수를 분리해서 보여준다. 한 학생이 여러 반을 들어 여러 행이 생겨도 운영 학생 수는 1명으로 계산해야 정원 마감 오해를 줄일 수 있다.
- 수강생 장부는 월별 반복 행을 같은 학생 ID에 귀속한다. 최신 월이 모두 휴원이면 현재 상태는 `PAUSED`, 과거 월 퇴원 후 이후 월 기록이 없으면 현재 상태는 `WITHDRAWN`으로 유지한다.
- 학생 상세 화면은 최신 완료 수강생 이관 배치의 `StudentRegistrationLedger`를 월별로 묶어 개인별 수강/결제/셔틀 히스토리를 보여준다.
- 학생 상세 화면의 운영 요약은 현재 상태, 최신 월 수강, 미납 금액, 청구서 상태와 납부 링크를 먼저 보여준다. 상담 중 필요한 정보는 긴 히스토리 표보다 위쪽 요약에서 바로 판단할 수 있어야 한다.
- 현재 수강 상태(`Enrollment.status = ACTIVE`)는 최신 완료 수강생 배치의 최신 월 기준으로 맞춘다. 2026년 전체 월별 반복 행은 학생별 히스토리로 보존하고, 시간표 인원에는 최신 월만 반영한다.
- 관리자 수납 월별 청구서는 `청구 대상 확인 → 청구서 생성` 흐름으로 분리한다. 생성 전 대상 학생, 금액, 기존 유지 건을 먼저 확인하고, 청구서 생성 시 학부모 SMS는 자동 발송하지 않는다.
- 수강생 시트 결제수단은 운영 의미로 해석한다. `랠리즈/카드결제/현금영수증`은 납부 완료, `미납/미결제`는 미납, `이월`은 부상·질병 등 결석 보전으로 기존 미납 청구가 있으면 `CANCELED/CARRY_OVER`로 닫는다.
- 현장 토스 단말기 결제는 온라인 PG 승인과 분리해 `PaymentTransaction.provider = TOSS_TERMINAL`로 기록한다. 단말기 승인번호는 `Payment.providerOrderId`와 감사 로그 metadata에 남겨 나중에 오프라인 거래내역과 대조할 수 있게 한다.
- 관리자 신청/체험 화면은 `slotKey`나 원문 에러 대신 요일·시간·반 이름, 처리 필요 배지, 쉬운 재시도 안내를 보여준다. 관리자가 볼 화면에는 개발 과정의 문구를 남기지 않는다.
- 관리자 신청/체험 목록은 학생명, 보호자명, 전화번호, 학교, 메모로 검색할 수 있어야 하며, 반복 처리 결과는 `alert`보다 화면 안 성공/오류 메시지로 남긴다.
- 관리자 신청/체험의 우선 처리 기준은 장기 대기(24시간 이상), 중복 연락처, 체험 후속 안내 미완료, 쌤 알림 미발송이다. 이 기준은 DB 상태 변경 없이 화면 계산으로 먼저 보여준다.
- 미납 알림은 청구서 생성과 분리된 별도 버튼으로 처리한다. 자동 발송보다 관리자 확인 후 발송하는 방식이 실사용 안정성이 높다.
- 학생 상세 화면에서는 수강 반별 `ACTIVE/PAUSED/WITHDRAWN` 상태를 직접 변경할 수 있다. 서버 액션에서 허용 상태값만 검증하고 관련 캐시를 무효화한다.
- 관리자 수납/결제 화면에는 최신 시트 저장 배치 기준 수납 대조 기능이 있다. 생성/수정/동일/확인 필요를 분리하고, 확인 필요 건은 자동 변경하지 않는다.
- 시간표는 `ScheduleSlot` DB 원본을 우선 사용한다. DB 데이터가 없을 때만 기존 시트 캐시로 fallback한다.
- 2026년 수강생 데이터는 스프레드시트 원본 보존 모델과 실제 `Student`/`Enrollment` 운영 모델을 함께 사용한다.

## 목차
- [architecture.md](architecture.md): 프로젝트 구조와 주요 기능
- [conventions.md](conventions.md): 작업, 코딩, 디자인 규칙
- [decisions.md](decisions.md): 기술 결정 이력
- [errors.md](errors.md): 에러와 주의할 함정
- [lessons.md](lessons.md): 작업 중 배운 교훈

## 현재 요약
- 2026-07-20: 방학특강 공개 신청·관리자 운영·정원/대기 데이터 구조 추가.
- DB는 Supabase PostgreSQL이고 Prisma를 사용한다.
- Supabase PgBouncer 호환 때문에 DB 구조 보강은 raw SQL 보장 패턴을 자주 사용한다.
- 공개 홈페이지와 관리자 페이지가 같은 Next.js 앱 안에 있다.
- 관리자 속도 개선 방향은 서버 초기 payload 축소, 지연 로딩, 서버 캐시, 불필요한 prefetch 제거를 우선한다.
- 스프레드시트는 2026년 7월 기준 이관 자료로 사용하고, 8월 이후 운영은 DB 중심으로 전환하는 방향이다.

## 2026-07-15 추가 지식
- 결제 운영은 기존 Payment 153건을 PaymentInvoice와 1:1로 백필했고, PaymentTransaction/PaymentWebhookEvent/PaymentAuditLog로 온라인 납부와 추적 기록을 분리한다.
- 학생별 2026년 월별 원장 데이터는 `Enrollment` 인원 계산과 분리해 상세 화면의 개인별 히스토리로 확인한다. 최신 완료 import 배치만 사용해 반복 import 중복을 피한다.
- 학생 상세 운영 요약은 `PaymentInvoice.checkoutUrl`을 이용해 납부 링크를 바로 열 수 있게 하고, 청구서가 없는 과거 수납은 "없음"으로 구분한다.
- 2026-07-15: 교사용 수업 화면의 학생 연락·청구 처리 성능 및 개인정보 캐시 금지 결정을 `decisions.md`에 기록.
- 2026-07-15: 매장 단말기 수납은 관리자 확인형 수동 반영으로 먼저 운영하고, 토스 오프라인 단말기 거래내역 API가 확인되면 승인번호 기준 자동 대조로 확장한다.
- 2026-07-15: 교사용 모바일 다이얼로그 접근성과 지연 로딩 규칙을 `conventions.md`에 기록.

> 2026-07-21 갱신: 셔틀 승객 운행 상태는 ShuttleRoutePassenger.rideStatus에 PENDING/BOARDED/DROPPED_OFF/NO_SHOW로 저장하고, 기사 앱에서 즉시 체크하며 관리자 셔틀 화면에서 상태 배지로 확인한다.
