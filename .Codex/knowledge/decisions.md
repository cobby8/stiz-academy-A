# Decisions

## 2026-09-02: 본사 카페24 결제는 서명된 브리지로 연결한다
- 결정: 다산점 청구서는 `PAYMENT_PROVIDER=CAFE24_BRIDGE`일 때 본사 결제 브리지 API로 서명 요청을 보내고, 본사가 돌려준 `checkoutUrl`로 학부모를 이동시킨다.
- 결정: 기본값은 기존 토스페이먼츠 직접결제(`PAYMENT_PROVIDER=TOSS`)로 유지해 환경변수 전환 전 운영 동작이 바뀌지 않게 한다.
- 결정: 카페24 납부 완료는 본사에서 보내는 서명 웹훅을 통해서만 `Payment`, `PaymentInvoice`, `PaymentTransaction`에 반영한다. 주문번호와 금액이 맞지 않으면 완료 처리하지 않는다.
- 이유: 카페24는 쇼핑몰 주문/결제 시스템이라 다산점이 결제창만 직접 띄우는 방식보다, 본사가 주문과 결제 링크를 만들고 다산점은 청구 장부와 결과만 맞추는 구조가 안전하다.

## 2026-09-01: Solapi 대량 문자는 한 번 접수하고 최종 결과를 재조회한다
- 최대 500명은 `POST /messages/v4/send-many/detail` 한 요청으로 접수하고 `customFields.deliveryId`로 사이트 장부 행과 연결한다.
- Solapi `ACCEPTED`는 최종 성공이 아니므로 장부 `SENDING`을 유지하고 `providerStatus=ACCEPTED`로 구분한다. 최종 `SENT/FAILED`는 그룹 결과 조회 뒤에만 확정한다.
- 4xx 명시 거절만 `FAILED`, 5xx·타임아웃·응답 누락은 자동 재발송하지 않고 `UNCERTAIN` 또는 재조회 대기로 둔다.
- 첫 결과 누락은 공급자 반영 지연일 수 있으므로 10분간 재조회하고, 이후에도 확인되지 않을 때만 `UNCERTAIN`으로 격리한다.
- Solapi가 아닌 Bizppurio는 기존 단건 발송 계약을 유지한다.

## 2026-08-31: 운영 동기화 워커는 먼저 읽기 전용 분류기로 둔다
- 결정: `/api/cron/operations-sync`는 `CRON_SECRET` 인증 후 PENDING/FAILED 시도를 읽기 전용으로 분류하고, 외부 시트·Rallyz·홈페이지 쓰기 함수는 호출하지 않는다.
- 결정: 자동 cron 등록은 하지 않는다. 시트 반영 준비, Rallyz 확인 대기, 홈페이지 반영 준비를 응답으로 나눠 운영자가 다음 조치를 판단하게 한다.
- 이유: 운영 동기화는 학생 상태와 외부 장부를 바꾸는 작업이라, 실행기만 붙였다고 바로 배송까지 보내면 승인 경계가 흐려진다. 먼저 분류기를 두면 어떤 대상이 안전하게 처리 가능한지 볼 수 있고, 이후 자동 실행 모드를 좁게 켤 수 있다.

## 2026-08-31: 유니폼 추가주문은 학원 DB 접수 후 STIZ 본사 API로 전송한다
- 결정: 유니폼 추가주문은 외부 구글폼 대신 `/apply/uniform` 자체 폼에서 접수하고 `UniformOrder`·`UniformOrderItem`에 먼저 저장한다.
- 결정: 본사 전송은 브라우저가 아니라 서버가 `STIZ_PARTNER_SECRET`으로 HMAC-SHA256 서명해 `custom.stiz.kr/api/orders`로 보낸다.
- 결정: 학원 화면에서는 금액을 계산하거나 결제받지 않고, 본사 접수번호와 전송 상태만 관리한다.
- 결정: 형제·자매 주문은 한 주문의 `items` 배열에 함께 담고, 재시도할 때도 같은 `partnerRequestId`를 사용한다.
- 결정: Vercel 빌드와 릴리스 프리플라이트에서 `UniformOrder` 구조, 고유키, RLS, anon/authenticated 직접 권한 차단을 읽기 전용으로 확인해 DB가 뒤처진 배포를 막는다.
- 이유: 주문 원장을 먼저 남기면 전송 실패와 재시도를 추적할 수 있고, 같은 접수번호를 쓰면 새 주문과 문자가 중복 생성되는 위험을 줄일 수 있다.

## 2026-08-29: Codex 운영 지식은 저장소 공유, 예약 실행기는 한 대
- 결정: 작업 규칙·STIZ 운영 스킬·인수인계 상태를 저장소에 포함해 두 Codex가 같은 기준을 읽는다.
- 결정: 비밀번호·키·쿠키·DB URL 같은 비밀정보는 각 PC의 로컬 환경에만 두고 Git으로 복제하지 않는다.
- 결정: 앱 수준 실행 lease가 구현되기 전에는 실제 외부 대조·발송 예약을 한 PC에서만 `ACTIVE`로 둔다.
- 이유: 대화 기억이나 브라우저 세션은 PC마다 다르며, 두 실행기가 동시에 작동하면 중복 청구·문자가 발생할 수 있다.

## 2026-07-25: 방학특강 출석 슬롯 생명주기 — 출결 불가침 + 소프트 취소
- 결정 (제1원칙): `SpecialProgramEnrollmentDate.attendanceStatus IS NOT NULL` 인 행은 **어떤 자동 로직도 건드리지 않는다.** 삭제·취소·kind 변경 모두 금지이며, 이 원칙은 코드 분기가 아니라 **모든 쓰기 SQL의 WHERE 조건**으로 강제한다.
- 이유: 출결은 되돌릴 수 없는 운영 기록이다. 코드 분기는 새 경로가 생기면 뚫리지만 WHERE 조건은 뚫리지 않는다.
- 결정: 슬롯은 하드 DELETE 하지 않는다. 이탈분은 `status='CANCELLED'` 소프트 취소만 한다.
- 이유: 되돌리기(A반→B반→A반)와 사고 복구가 가능해야 한다. 복구는 `CANCELLED → SCHEDULED` 되살리기로 처리한다.

### 확정된 운영 규칙 4가지 (사용자 승인, 2026-07-25)
1. **보강 날짜가 그 학생의 원래 수업일과 겹치면 막는다.** ("그 날은 원래 수업이 있는 날입니다") — UI 후보 목록뿐 아니라 저장 경로(`createMakeup`)에서도 검사한다. 관리자·학부모 두 경로가 같은 함수를 타므로 한 곳만 막으면 양쪽이 막힌다.
2. **반 이동 시 예전 반 예정일은 자동 정리(소프트 취소)하되, 출결 찍힌 날은 보존한다.** 완전 삭제 금지.
3. **출결 찍힌 학생의 반 이동은 허용한다.** 막지 않는다. 다만 출결이 찍힌 날은 예전 반에 그대로 남고, 그 건수를 감사로그(`ITEM_ASSIGNMENT_UPDATED`의 `enrollmentDatesResync.blockedByAttendance`)에 남긴다.
4. **보강을 이수해도 원래 결석은 결석으로 남긴다.** (현행 유지 — 결석 1건 + 보강 참석 1건, 두 줄로 관리)

### 파생 설계 결정
- 결정: 재동기화는 신청 항목(Item)이 아니라 **신청서(Application) 단위**로 실행한다.
- 이유: `selectedWeekdays`가 항목이 아니라 `SpecialProgramApplication`에 저장돼 있어서, 한 신청서에 반이 2개면 한쪽만 고쳐도 형제 항목 좌석이 낡아버린다.
- 결정: 보강 좌석 upsert(`ON CONFLICT ... DO UPDATE`)는 `status='CANCELLED' AND attendanceStatus IS NULL` 인 행만 재사용한다. 영향 행이 0이면 조용히 넘어가지 않고 에러를 던져 보강 배정 자체를 되돌린다.
- 이유: 조용히 넘어가면 "배정됐다고 표시되는데 출석부에는 없는" 유령 보강이 생긴다.
- 결정: 보강 정원은 승인과 같은 **"linkedClass 형제 반 그룹 × 요일"** 기준으로 판정한다(`src/lib/seasonal/makeup-capacity.ts`).
- 이유: 같은 교실을 승인은 그룹 기준, 보강은 날짜 기준으로 재던 탓에 승인은 막히는데 보강은 통과하는 모순이 있었다.
- 결정: **이미 정원을 넘긴 반(예: 초등 고학년 월 15명 / 정원 12)에서는 보강을 차단하지 않고 경고 로그만 남긴다.** 여유가 남은 반만 (정규 + 기배정 보강)이 정원에 닿는 시점부터 막는다.
- 이유: 기존 초과분은 사용자가 "의도한 것/운영상 괜찮다"고 확인한 상태다. 여기서 엄격히 막으면 그 반은 보강이 영구히 불가능해져 운영이 마비된다.
- 결정: 목표 회차를 하나도 못 구한 경우(회차 미등록·요일 설정 오류)에는 재동기화를 건너뛰고 경고만 남긴다.
- 이유: 그대로 적용하면 기존 좌석이 통째로 취소돼 출석부가 비어버린다. 설정 오류의 대가가 데이터 소실이면 안 된다.
- 적용 시점 근거: 프로덕션 실측상 슬롯 185행 전부 `REGULAR/SCHEDULED`, 출결 0건·보강 0건·CANCELLED 0건이라 이 변경으로 손상될 기존 데이터가 없었다(무손실 수정 적기). 스키마 변경·마이그레이션은 하지 않았다.

## 2026-07-22: 관리자 셔틀 위치 수정은 기존 신청 좌표 칸을 갱신한다
- 결정: 새 테이블이나 컬럼을 만들지 않고 `SpecialProgramShuttleRequest`의 pickup/dropoff 주소·좌표·confirmedAt 필드를 관리자 핀 저장에도 그대로 사용한다.
- 이유: 학부모 신청 지도 위치와 관리자 보정 위치가 같은 운행 기준점이므로, 노선 배정 전에는 한 원본 위치만 최신값으로 관리하는 편이 단순하고 안전하다.
- 적용: `/admin/shuttle` 미배정 학생 카드의 `위치 찍기` 버튼에서 카카오 지도 모달을 열고, 저장 시 `SHUTTLE_LOCATION_CONFIRMED` 감사 로그를 남긴다.

## 2026-07-22: 셔틀 운행 완료는 보관과 별도 상태로 기록한다
- 결정: 확정 노선을 실제 운행 종료 후 `COMPLETED` 상태로 전환하고, `completedAt`/`completedByUserId`를 함께 남긴다.
- 이유: `ARCHIVED`는 숨김/정리 의미가 강하고, 운행 완료는 정산·학부모 알림·기사 체크 이력의 기준점이므로 별도 상태가 필요하다.
- 적용: 관리자 셔틀 화면에서 체크 대기 학생이 0명일 때만 운행 완료를 누를 수 있다.

## 2026-07-21: 셔틀 기사를 별도 `DRIVER` 역할로 분리한다
- 결정: 기사 계정은 기존 코치/강사(`INSTRUCTOR`)에 섞지 않고 `Role.DRIVER`로 분리한다.
- 이유: 수업 출결, 학생 관리, 청구, 사진 업로드는 선생님 업무이고, 셔틀 운행은 노선·탑승 체크 중심 업무라 기본 화면과 권한이 다르다.
- 결정: 가입 방식은 별도 공개 회원가입이 아니라 기존 스태프 개인 초대 링크를 재사용한다.
- 이유: 관리자가 초대한 사람만 내부 앱에 들어오게 만드는 흐름은 코치/강사와 기사 모두 동일하게 안전하다.
- 결정: `/staff/shuttle`은 우선 기사 모바일 홈의 진입점과 UI 기반만 제공하고, 노선 배정·실시간 체크는 다음 작업에서 셔틀 데이터 모델과 연결한다.
- 이유: 역할/로그인/초대 기반을 먼저 안정화해야 이후 운행 기능을 붙일 때 권한 경계가 흔들리지 않는다.

## 2026-07-21: 셔틀 노선은 `driverUserId`로 기사에게 직접 배정한다
- 결정: 별도 기사 프로필 테이블을 먼저 만들지 않고 `ShuttleRoutePlan.driverUserId -> User.id` 관계로 노선을 배정한다.
- 이유: 현재 기사 계정은 `User.role = DRIVER`로 충분히 구분되며, 운행표 확인에 필요한 최소 정보는 이름과 전화번호다. 별도 프로필은 근무 가능 시간, 차량 소속, 면허 정보가 필요해질 때 확장한다.
- 결정: 확정 노선은 차량, 담당 기사, 탑승 학생이 모두 있어야 확정할 수 있게 한다.
- 이유: 기사 앱은 확정 노선만 읽기 때문에, 확정 시점에 운행 책임자와 실제 탑승자가 비어 있으면 현장 운영표가 불완전해진다.

## 2026-07-21: 방학특강 일괄 처리는 신청 반 항목 단위로 처리한다
- 결정: 방학특강 관리자 목록에서 학생 행을 선택하더라도 실제 상태 변경은 `SpecialProgramApplicationItem` 단위로 처리한다.
- 이유: 한 학생이 여러 방학특강 반을 신청할 수 있어서, 신청서 전체를 한 번에 승인하면 반별 정원, 대기순번, 반려/취소 상태가 섞일 수 있다. 실제 좌석은 신청 반에 붙어 있으므로 승인도 좌석 단위로 처리해야 한다.
- 결정: 일괄 처리는 한 번에 최대 100개 신청 항목으로 제한하고, 각 항목을 `Serializable` 트랜잭션에서 처리해 부분 성공/실패 결과를 반환한다.
- 이유: 정원 확인은 매표소에서 남은 좌석을 확인하는 것과 같아서 동시에 여러 관리자가 누르면 꼬일 수 있다. 항목별 잠금과 부분 결과를 쓰면 성공한 건은 저장하고, 실패한 건은 사유와 함께 다시 선택 상태로 남길 수 있다.
- 결정: 검색/필터로 현재 보이는 목록만 전체 선택 대상으로 삼는다.
- 이유: 화면에 안 보이는 학생까지 몰래 처리되면 운영자가 실수하기 쉽다. 일괄 처리 범위는 눈앞에 보이는 목록으로 제한하는 편이 안전하다.

## 2026-07-21: 체험/수강신청 취소는 삭제하지 않고 상태로 보존한다
- 결정: 체험 신청은 `TrialLead.status = CANCELLED`, 수강신청은 `EnrollmentApplication.status = CANCELLED`로 남기고, 사유는 기존 처리 메모/이탈 사유 필드에 기록한다.
- 이유: 신청 취소는 운영 이력이다. 삭제하면 어떤 학부모가 언제 신청했다가 왜 취소했는지 사라지고, 중복 신청이나 재신청 상담 때 맥락을 잃는다.
- 결정: 승인된 수강신청은 신청서 수정 모달에서 직접 수정하지 않고 원생/수강 등록 메뉴에서 변경한다.
- 이유: 승인 후에는 신청서가 이미 `Student`, `Enrollment`, 청구 흐름으로 변환된다. 원본 신청서만 바꾸면 실제 운영 데이터와 화면 표시가 엇갈릴 수 있다.

## 2026-07-16: 수강생 시트 결제수단은 운영 의미대로 분리해 반영한다
- 결정: 시트의 `랠리즈`, `카드결제/카드`, `현금영수증/현금`은 모두 `PAID`로 반영하고, `미납`과 기존 표기 `미결제`만 `PENDING`으로 반영한다.
- 이유: 결제수단 칸은 실제 수납 장부와 같아서, 납부 경로가 다를 뿐 랠리즈·카드·현금영수증은 모두 수납 완료다. 반대로 미납/미결제만 학부모 청구와 미납 알림 대상이어야 한다.
- 결정: 시트의 `이월`은 새 미납 청구를 만들지 않고, 같은 월에 기존 미납/연체 청구가 있으면 `Payment.status = CANCELED`, `method = CARRY_OVER`로 닫아 `취소/이월`로 표시한다.
- 이유: 이월은 부상·질병 등으로 지난달 수강료를 보전하는 기록이지 미납이 아니다. 기존 청구를 그대로 두면 관리자와 학부모 화면에서 실제로 내야 할 돈처럼 보이므로, 닫힌 청구로 처리해야 한다.
- 결정: `휴원`은 해당 월의 `PAUSED`, `퇴원`은 해당 월의 `WITHDRAWN` 이력으로 보존하고, 이후 월 데이터가 없는 것은 정상으로 본다.
- 이유: 학생이 그만둔 뒤 시트에 이후 월 행이 없는 것은 누락이 아니라 운영 상태의 결과다. 현재 인원 계산은 최신 월 기준으로 하고, 과거 월 상태는 학생 상세 히스토리에 남긴다.

## 2026-07-16: 관리자 신청/체험 긴 목록은 서버 페이지 단위로 나눠 로딩한다
- 결정: `/admin/apply` 첫 진입에서는 체험 문의 최근 50건과 수강신청 통계만 먼저 가져오고, 수강신청 목록은 해당 탭 진입 시 50건 단위로 요청한다.
- 이유: 선생님이 처음 보는 화면에는 오늘 처리할 요약과 최근 항목만 필요하다. 전체 신청/체험 데이터를 한 번에 가져오면 데이터가 누적될수록 첫 진입이 다시 느려진다.
- 결정: `getTrialLeads`와 `getEnrollApplications`는 기존 상태 필터 호출을 유지하면서 `limit/offset` 옵션도 지원한다.
- 이유: 기존 호출부를 깨지 않으면서 관리자 화면만 더 가볍게 만들 수 있다.
- 결정: 통계는 전체 기준으로 유지하고, 목록은 부분 로딩 상태를 인정한다. 필터 결과가 현재 묶음에 없어도 다음 묶음이 남아 있으면 “다음 50건에서 더 찾아보기”를 제공한다.
- 이유: 통계 숫자는 운영 판단에 필요하지만, 목록 전체를 첫 화면에 싣는 것은 속도에 불리하다.

## 2026-07-16: 신청/체험 연락 기록은 별도 이력 테이블로 분리한다
- 결정: 체험 문의와 수강신청에 연락 상태 컬럼을 계속 붙이지 않고, `ApplicationContactLog`에 연락 완료, 부재, 재연락 예약, 상담 메모를 누적한다.
- 이유: 한 신청 건에 여러 번 연락할 수 있으므로 원본 신청서 테이블은 서류철로 유지하고, 연락 이력은 메모지처럼 따로 쌓는 구조가 운영 히스토리를 잃지 않는다.
- 결정: 관리자 목록 조회에는 전체 로그를 싣지 않고 최신 연락 1건과 열린 재연락 1건만 LATERAL join으로 붙이며, 읽기 경로에서 테이블 생성/컬럼 보장 DDL은 실행하지 않는다.
- 이유: 관리자 속도 개선을 유지하려면 목록 payload와 DB 작업 수를 작게 유지해야 한다. 테이블 보장은 쓰기 액션과 운영 SQL에 맡기고, 아직 테이블이 없으면 기존 목록으로 fallback한다.

## 2026-07-16: 교사 가입은 관리자 개인 초대 링크로만 시작한다
- 결정: 공용 설치 화면이나 전화번호 조회로 교사 셀프가입을 열지 않고, 원장이 생성한 7일 유효 개인 초대와 전화 OTP를 유지한다.
- 이유: 설치 주소는 누구나 알 수 있으므로 여기서 교사 권한 계정을 만들게 하면 권한 오남용과 계정 열거 위험이 생긴다.
- 결정: SMS 발송 결과와 초대 생성 결과를 분리하고, 문자 실패 시에도 관리자가 개인 가입 URL을 복사·공유할 수 있게 한다.
- 이유: 문자 서비스 장애가 가입 진입점 자체를 없애지 않도록 하기 위해서다.

## 2026-07-15: 교사용 앱 설치 링크와 개인 초대 링크를 분리한다
- 결정: 반복 공유 가능한 설치 링크는 `/staff/install` 하나로 고정하고 초대 토큰을 manifest, 앱 시작 주소, 브라우저 저장소에 넣지 않는다.
- 이유: 초대 링크는 한 사람의 가입 권한을 가진 개인 열쇠이고 설치 링크는 여러 선생님에게 공유하는 안내 주소이므로 섞으면 토큰 노출과 재사용 문제가 생긴다.
- 결정: 설치 여부를 localStorage에 영구 기록하지 않고 현재 standalone 상태와 현재 세션의 설치 완료 이벤트만 사용한다.
- 이유: 앱 삭제 뒤 저장값만 남아 재설치를 막는 오판을 방지하기 위해서다.

## 2026-07-15: 교사용 앱은 `/staff` 범위의 독립 PWA로 제공한다
- 결정: 관리자 화면과 분리된 `/staff`를 공식 진입점으로 두고, 로그인도 `/staff/login` 주소 안에서 처리한다.
- 이유: 홈 화면에 설치한 앱에서 세션이 만료되어도 일반 브라우저 화면으로 튀지 않고 교사용 흐름을 계속 유지하기 위해서다.
- 결정: 교사용 수업·학생·청구 데이터는 오프라인 캐시에 저장하지 않는다.
- 이유: 휴대폰 분실이나 공용 기기 사용 때 학생과 학부모 개인정보가 기기에 남는 위험을 줄이기 위해서다.
- 결정: 카메라·마이크 권한은 교사용 경로에만 허용한다.
- 이유: 사진 등록과 음성 메모에는 필요하지만 관리자·공개 화면까지 권한 범위를 넓힐 이유는 없기 때문이다.

## 2026-07-15: 현재 수강 상태는 최신 월 시트 기준으로 맞춘다
- 결정: 2026년 전체 수강생 이력은 `StudentRegistrationLedger`에 계속 보존하되, 시간표와 현재 반 인원에 쓰는 `Enrollment.status = ACTIVE`는 최신 완료 배치의 최신 월만 기준으로 정합성을 맞춘다.
- 이유: 1월부터 7월까지 같은 학생이 반복 등장하는 것은 수강 이력이지 현재 인원이 아니다. 현재 반 인원은 “오늘 출석부”처럼 최신 월만 봐야 하며, 과거 월 ACTIVE를 그대로 두면 정원이 마감된 것처럼 보인다.

## 2026-07-15: 수납 원장 반영은 관리자 확인 후 적용한다
- 결정: 최신 시트 원장을 기준으로 수납 기록을 바로 자동 변경하지 않고, 관리자 수납/결제 화면에서 생성·수정·확인 필요 건수를 먼저 보여준 뒤 `변경 적용`을 눌렀을 때만 반영한다.
- 이유: 수강료 청구와 미납 알림은 돈과 직결되므로 휴원, 퇴원, 추가수강, 이월처럼 자동 판단이 애매한 행을 섞어 처리하면 잘못된 청구가 나갈 수 있다. 그래서 확실한 납부/미납 건만 자동 적용 대상으로 두고, 애매한 건은 관리자 확인 대상으로 남긴다.

## 2026-07-13: 월별 수강생 중복은 연락처 기반으로 우선 매칭한다
- 결정: Google Sheets 월별 데이터에 같은 학생이 반복 등장할 때는 아이 이름만으로 확정 연결하지 않고, 아이 이름과 학부모 연락처, 학생 연락처, 생년월일을 우선순위로 기존 `Student`와 매칭한다. 이름만 유일하게 맞는 경우는 약한 후보로 표시하되 자동 적용하지 않는다.
- 이유: 계속 다니는 아이들은 여러 월 시트에 반복되지만, 이름만으로 합치면 동명이인이나 형제 계정이 섞일 수 있다. 연락처와 생년월일은 학생을 구분하는 신분증 같은 역할을 하므로 자동 이관은 강한 근거가 있을 때만 진행하고, 애매한 행은 관리자 확인 대상으로 남기는 편이 운영 데이터가 안전하다.

## 2026-07-12: Move schedule operation from sheet cache toward DB-first slots
- Decision: Add `ScheduleSlot`, `ScheduleImportBatch`, and `ScheduleImportIssue` as the DB-first source for academy schedule operations. Keep `SheetSlotCache`, `ClassSlotOverride`, and `CustomClassSlot` as legacy inputs during migration, then import them into `ScheduleSlot`.
- Reason: The academy already runs enrollments, attendance, payments, trials, waitlists, and makeup sessions through DB tables, but the schedule source still depends on a sheet cache. A dedicated slot table makes the schedule the same kind of durable operational record as the rest of the academy system.
- Security note: New public-schema Supabase tables should not be assumed to be exposed automatically. The SQL setup enables RLS and revokes anon/authenticated Data API access until an explicit public policy is needed.

## 2026-07-12: 다크모드 브랜드 강조색은 주황이 아니라 라임으로 치환한다
- 결정: 라이트모드의 브랜드 강조색은 기존 주황을 유지하되, 다크모드에서는 `--brand-accent`를 `brand-neon-lime`으로 정의한다. 전역 안전망은 `brand-orange`/`orange` 배경·텍스트·테두리·hover·focus 누락분을 라임 계열로 바꾸고, 라임 배경 위 흰 글자는 남색 글자로 보정한다.
- 이유: 다크모드에서 주황을 단순히 어둡게 만들면 브랜드 체계가 흔들리고, 화면마다 갈색/주황 잔상이 남는다. 라이트=주황, 다크=라임이라는 규칙을 하나의 수도꼭지처럼 `--brand-accent`에 모으면 컴포넌트가 놓친 부분도 일관되게 따라간다.

## 2026-07-12: 브라우저/PWA 테마 색상도 앱 테마와 함께 움직인다
- 결정: 정적 `meta[name="theme-color"]`와 `manifest.json` 기본색은 다크모드 기준의 라임 강조색으로 두고, `ThemeColorUpdater`가 `next-themes`의 `resolvedTheme`에 맞춰 브라우저/PWA 헤더 색상을 런타임에 갱신한다.
- 이유: 홈페이지 기본 테마를 다크모드로 바꿔도 브라우저 헤더가 주황색으로 남으면 앱 전체가 밝은 모드처럼 보인다. 테마 색상은 앱의 외곽 프레임 색이므로, CSS 화면만이 아니라 브라우저가 읽는 메타 정보도 같이 맞춰야 한다.

## 2026-07-12: 다크모드 칩/뱃지 대비는 전역 안전망으로 보정한다
- 결정: 사이트와 관리자 전역에서 반복되는 `bg-surface-warm`, `bg-surface-section`, `bg-*-50/100`, `bg-*-50/50`, `text-*-700`, `border-*-200` 같은 밝은 surface와 pastel 상태 칩/뱃지는 `src/app/globals.css`의 `.dark ...:not([class*="dark:*"])` 안전망으로 보정한다. 이미 명시적인 `dark:bg-*`, `dark:text-*`, `dark:border-*`가 있는 컴포넌트는 건드리지 않는다.
- 이유: 같은 종류의 칩/뱃지가 사이트와 관리자 여러 화면에 퍼져 있어 페이지마다 수정하면 빠뜨리기 쉽다. 전역 안전망은 안전벨트처럼 빠진 곳만 잡아주고, 의도적으로 다크 스타일을 지정한 컴포넌트는 그대로 존중한다.

## 2026-07-12: 사이트 운영 점검 봇은 새벽 자동 실행과 수동 실행을 함께 둔다
- 결정: 사이트 운영 점검 봇은 관리자 대시보드 수동 버튼을 유지하면서, Vercel Cron으로 매일 UTC 17:00, 한국 시간 새벽 2시에 `GET /api/cron/site-ops-bot`가 자동 실행되도록 등록한다. cron endpoint는 기존 cron 패턴처럼 `CRON_SECRET` bearer 인증을 요구하고, 관리자 버튼과 같은 `runSiteOpsBot` 로직을 호출한다.
- 이유: 점검 자체는 매일 자동으로 돌아야 운영 누락을 빨리 발견할 수 있지만, 관리자 첫 진입 때 자동 실행하면 다시 느려질 수 있다. 새벽 2시는 선생님 사용량이 낮고, 기존 백업 cron이 KST 자정에 돈 뒤라 백업 상태까지 함께 확인하기 좋다.

## 2026-07-12: 사이트 운영 점검 봇은 관리자 첫 화면에서 자동 실행하지 않는다
- 결정: `/admin` 첫 진입의 사이드바 기본 탭은 사이트로 바꾸고, 관리자 화면에서는 대시보드 카드의 “점검 실행” 버튼을 눌렀을 때만 사이트 운영 점검 봇을 실행한다. 점검 봇은 DB 연결, 기본 설정, 공개 콘텐츠, 시간표, 신청 링크, 백업 저장소, 인스타 자동 게시 설정과 대기열을 점검한다. 자동 조치는 기본 설정 row 생성과 백업 bucket 생성처럼 되돌릴 위험이 낮은 항목으로 제한하고, 수동 확인이 필요한 항목은 관리자/부관리자 알림으로 남긴다.
- 이유: 운영 점검은 유용하지만 첫 화면 진입마다 백업 Storage, 인스타 큐, 여러 콘텐츠 카운트를 동시에 확인하면 관리자 속도가 다시 느려질 수 있다. 관리자 화면의 점검은 건강검진 버튼처럼 필요할 때 실행하고, 정기 점검은 새벽 cron에 맡기는 구조가 속도와 안정성의 균형이 좋다.

## 2026-07-12: 관리자 긴 목록은 화면과 DB 양쪽에서 점진화한다
- 결정: `/admin/students`는 초기 부분 데이터가 있으면 자동으로 전체 목록을 다시 가져오지 않고, 사용자가 “전체 목록 불러오기”를 눌렀을 때만 전체 원생을 조회한다. `/admin/trial`과 `/admin/apply`는 처음 50건만 렌더링하고 더 보기로 추가 표시한다. 수납/알림/피드백/체험/대기/보강/등록 통계처럼 관리자 화면에서 자주 쓰는 조회 패턴은 Prisma schema와 SQL 파일에 인덱스를 남긴다.
- 이유: 선생님이 첫 화면에서 필요한 것은 최근/핵심 목록이고, 수강생 전체나 오래된 신청 전체는 필요할 때만 보면 된다. 긴 목록을 한 번에 그리고, 동시에 DB가 정렬/필터를 매번 새로 훑으면 브라우저와 DB가 함께 느려진다. 목록은 앞줄부터 보여주고, DB에는 자주 찾는 순서표를 붙이는 방식이 체감 속도에 유리하다.

## 2026-07-12: 홈페이지 기본 테마는 다크모드로 시작한다
- 결정: `next-themes`의 `defaultTheme`을 `dark`로 설정해, 저장된 사용자 선택이 없는 첫 방문자는 다크모드로 시작하게 한다. `enableSystem`은 유지해 기존 테마 처리와 향후 시스템 옵션 호환성을 남긴다.
- 이유: 홈페이지를 기본적으로 다크모드 경험으로 보여주려면 OS 설정을 따르는 `system`보다 명시적인 `dark`가 안정적이다. 사용자가 토글로 라이트모드를 선택하면 `next-themes`가 저장해 두므로, 기본값 변경이 사용자의 직접 선택을 덮어쓰지는 않는다.

## 2026-07-12: 다크모드 기본색은 전역 body가 아니라 CSS 변수와 안전망으로 관리한다
- 결정: `body`에는 다크모드 배경/글자 유틸리티를 직접 넣지 않고, `.dark`에서 `--background`, `--foreground`, `color-scheme`을 지정한다. 반복적으로 누락되는 input/select/textarea, 기본 gray/white 배경, hover 색상은 `globals.css`의 다크모드 대비 안전망으로 보정한다.
- 이유: `body`에 `dark:bg-brand-neon-lime dark:text-brand-navy-900`가 들어가면 페이지 전체의 기본 색이 네온 라임/남색으로 상속되어, 색을 명시하지 않은 영역에서 글자가 배경에 묻힌다. 전역 기본색은 조명 스위치처럼 모든 화면에 영향을 주므로, 브랜드 강조색이 아니라 안정적인 어두운 배경과 밝은 글자를 기본값으로 둔다.

## 2026-07-12: 관리자 이미지 목록은 처음 일부만 렌더링한다
- 결정: `/admin/gallery`는 서버 초기 payload가 있으면 클라이언트에서 즉시 `/api/admin/gallery`를 다시 호출하지 않고, 갤러리 카드는 최초 12개만 렌더링한 뒤 “더 보기”로 점진 표시한다.
- 이유: 갤러리 관리자는 이미지 썸네일과 카드 버튼이 많아 첫 화면에서 DOM과 이미지 요청이 한꺼번에 늘어난다. 선생님이 처음 보는 것은 최근 게시물 일부면 충분하므로, 긴 목록은 책장 전체를 한 번에 꺼내지 않고 앞줄부터 보여주는 방식이 체감 속도에 유리하다.

## 2026-07-12: 드문 관리자 도구는 클릭 후 로드한다
- 결정: 관리자 사이드바의 백업/복원/시트 동기화 도구는 `/admin` 첫 진입 때 바로 로드하지 않고, “시스템 도구” 버튼을 열 때만 `AdminBackupButtons` lazy chunk를 로드한다.
- 이유: 관리자 첫 화면은 선생님들이 매일 보는 업무 진입점이고, 백업/복원/수동 동기화는 필요할 때만 쓰는 보조 기능이다. 처음부터 함께 내려받으면 작은 기능이라도 첫 렌더 JS 부담이 늘어나므로, 자주 쓰지 않는 도구는 접힌 서랍처럼 필요할 때만 꺼내는 편이 체감 속도에 유리하다.

## 2026-07-12: Vercel Fluid Compute를 관리자 콜드스타트 완화 옵션으로 켠다
- 결정: `vercel.json`에 `"fluid": true`를 추가해 Vercel Functions에 Fluid Compute를 명시적으로 활성화한다.
- 이유: Vercel 문서상 Fluid Compute는 함수 pre-warming, Node.js bytecode caching, 기존 idle resource 우선 사용 등 콜드스타트 영향을 줄이는 기능을 제공한다. 관리자 페이지는 동적 함수와 DB 인증/조회가 필요한 구조라 첫 진입 지연 완화 효과가 크다.

## 2026-07-12: 관리자 첫 화면의 핵심 payload는 DB 왕복 수를 최소화한다
- 결정: `/admin` primary dashboard payload는 대시보드 카운트, 신청 통계, 오늘 수업을 각각 별도 Promise로 조회하지 않고 하나의 raw SQL 호출로 묶는다.
- 이유: 운영 계측상 관리자 첫 진입은 콜드스타트와 첫 DB 연결 지연이 크게 작용한다. 작은 쿼리라도 여러 DB 왕복이 겹치면 서버 함수가 화면 HTML을 반환하기 전에 기다리는 단계가 늘어나므로, 첫 화면에 필요한 핵심 숫자는 한 번에 가져오는 편이 낫다.

## 2026-07-12: Vercel Functions를 Supabase Seoul DB 가까운 `icn1`에서 실행한다
- 결정: `vercel.json`에 `regions: ["icn1"]`을 추가해 배포 함수의 기본 실행 리전을 서울로 고정한다.
- 이유: 실제 Chrome 로그인 세션에서 실서버 `/admin` 첫 진입은 DOM 9.46초였지만 재로드는 0.84초였고, 홈페이지는 0.35~0.56초였다. 관리자 페이지는 인증/DB 조회가 필요한 동적 함수라 함수 실행 지역과 Supabase DB 지역 거리가 콜드 스타트와 첫 DB 연결 지연을 크게 만든다. Vercel 공식 문서도 함수와 데이터소스를 가까운 리전에 두는 것을 권장한다.

## 2026-07-11: 자주 쓰는 관리자 화면은 캐시된 초기 payload를 서버에서 주입한다
- 결정: `/admin/students`, `/admin/classes`, `/admin/trial`의 읽기 payload를 `src/lib/adminReadPayloads.ts`로 공유하고, 페이지 서버 렌더링에서 초기 데이터로 넘긴다.
- 이유: DB 조회와 서버 캐시는 이미 충분히 빨라도, 브라우저가 첫 렌더 후 같은 API를 다시 호출하면 네트워크 왕복과 hydration 이후 대기 시간이 남는다. 자주 쓰는 메뉴는 첫 HTML/RSC 흐름에 데이터를 태워 보내는 편이 체감 속도에 유리하다.

## 2026-07-06: 프로젝트 정체성 확정
- 결정: 저장소는 STIZ 농구교실 다산점 홈페이지와 학원관리 플랫폼으로 본다.
- 이유: 실제 코드는 상품/장바구니/주문 쇼핑몰이 아니라 학원 운영 기능 중심이다.

## 2026-07-06: 메인 홈 갤러리 기준 데이터 통합
- 결정: 메인 홈 활동 사진 섹션은 `AcademySettings.galleryImagesJSON` 대신 공개 `GalleryPost` 데이터를 사용한다.
- 이유: `/gallery`와 메인 홈이 서로 다른 사진 저장소를 보면 관리자가 같은 사진을 두 번 관리해야 한다.

## 2026-07-06: 관리자 설정의 구 갤러리 입력 제거
- 결정: `/admin/settings`의 포토 갤러리 URL 입력은 제거하고 `/admin/gallery` 안내로 대체한다.
- 이유: 공개 홈과 `/gallery`가 이미 `GalleryPost` 기준으로 동작하므로, 더 이상 반영되지 않는 입력칸은 혼란을 만든다.

## 2026-07-06: 운영시간/정책/푸터/SNS 관리자 설정화
- 결정: 운영시간, 개인정보처리방침, 푸터 문구, SNS 링크는 `AcademySettings`에 저장하고 관리자에서 관리한다.
- 이유: 운영 문구는 자주 바뀔 수 있으므로 코드 배포 없이 관리자가 즉시 수정할 수 있어야 한다.

## 2026-07-06: 인스타그램 토큰은 환경변수로 분리
- 결정: Instagram Business Account ID와 자동 업로드 ON/OFF는 관리자 설정에 두되, API access token은 서버 환경변수에 둔다.
- 이유: access token은 민감 정보라 DB 텍스트 입력으로 저장하면 노출/백업 위험이 커진다.

## 2026-07-07: 인스타그램 가져오기는 공통 동기화 함수와 하루 1회 cron 사용
- 결정: 수동 버튼과 Vercel cron이 `syncInstagramGalleryPostsToDb`를 함께 사용하고, cron은 하루 1회 실행한다.
- 이유: 중복 저장, 캐시 갱신, 오류 처리를 한 곳에서 관리할 수 있고, 사용자가 하루 1회면 충분하다고 정했다.

## 2026-07-07: 선생님 인스타 자동화는 초안-승인 구조로 분리
- 결정: 선생님은 `/staff/quick-post`에서 AI 초안을 만들고, 실제 홈페이지/인스타 게시는 `/admin/gallery`에서 관리자 또는 부관리자가 승인한다.
- 이유: 여러 선생님이 활용하려면 업로드는 쉬워야 하지만, 외부 공개 게시물은 최종 검수 단계가 필요하다.

## 2026-07-08: 선생님 사진 업로드는 승인 없이 바로 게시
- 결정: 선생님은 본인이 만든 `/staff/quick-post` 초안을 직접 게시할 수 있고, 게시 시 홈페이지 갤러리에 먼저 반영한 뒤 인스타그램 게시를 시도한다.
- 이유: 운영 흐름상 매번 관리자 승인을 거치면 사진 업로드 속도가 떨어지므로, 선생님이 미리보기에서 문구를 확인하고 바로 게시하는 흐름을 기본으로 둔다. 관리자는 `/admin/gallery`에서 실패 또는 대기 초안을 보조 관리한다.

## 2026-07-08: 공지사항 홍보 발행은 갤러리와 분리
- 결정: 공지사항을 인스타 피드/스토리와 페이스북 광고 소재로 활용할 때는 `GalleryPost`를 만들지 않고 `SocialCampaignPost`에 별도 기록한다.
- 이유: 방학특강 같은 홍보 공지는 공개 갤러리 사진 기록과 성격이 다르다. 갤러리에 섞이면 학부모가 보는 활동 사진 흐름이 광고 게시물로 오염될 수 있으므로, 홍보 캠페인은 따로 관리한다.

## 2026-07-08: 이미지 저장 전 브라우저 압축 적용
- 결정: 주요 이미지 업로드는 서버 저장 전에 클라이언트 `canvas`로 JPG 리사이즈/품질 압축을 수행한다.
- 이유: 휴대폰 원본 사진을 그대로 저장하면 관리자와 공개 홈페이지가 원본 이미지를 많이 불러와 느려진다. 저장 전에 가볍게 만들면 이후 모든 화면의 이미지 다운로드 부담이 줄어든다.

## 2026-07-08: 인스타 게시는 홈페이지 갤러리 게시 후 후속 상태로 분리
- 결정: 선생님/관리자 초안 게시 시 `GalleryPost` 저장을 먼저 완료하고 `SocialPostDraft`를 `PUBLISHING`으로 둔 뒤 인스타그램 게시를 이어서 시도한다.
- 이유: Meta API 게시가 느리거나 일시 실패해도 홈페이지 갤러리 반영 성공 메시지는 즉시 보여줘야 실사용자가 안심하고 다음 작업을 할 수 있다. 남은 인스타 게시는 `PUBLISHED`/`FAILED` 또는 재시도 가능한 `PUBLISHING` 상태로 관리한다.

## 2026-07-08: 홈 히어로 공지는 가벼운 공개 목록으로 표시
- 결정: 홈 히어로에는 공개 공지의 제목, 고정 여부, 날짜만 최대 4개 표시하고 본문 HTML은 가져와도 렌더하지 않는다.
- 이유: 공지 노출은 첫 화면 정보성은 높이지만, 본문까지 렌더하면 모바일 첫 화면 DOM과 계산량이 늘어난다. 목록 링크만 제공하면 반응 속도를 유지하면서 공지 접근성을 높일 수 있다.

## 2026-07-09: 선생님 인스타 게시를 서버 큐와 cron으로 처리
- 결정: 홈페이지 갤러리 반영 후 `SocialPostDraft`를 `PUBLISHING` 상태로 두고, `/api/cron/social-posts`가 5분마다 1건씩 인스타 게시를 처리한다.
- 이유: Meta API 게시에는 미디어 처리 대기와 재시도가 있어 화면에서 기다리면 선생님/관리자 페이지가 느려진다. 서버 큐로 넘기면 사용자는 홈페이지 반영 성공을 즉시 확인하고, 브라우저를 닫아도 인스타 게시가 이어진다.

## 2026-07-09: 전역 폰트와 추적 스크립트는 첫 화면에서 가볍게 둔다
- 결정: 관리자에서 선택 가능한 Google 폰트들은 전역 등록을 유지하되 `preload: false`로 두고, Meta Pixel은 `NEXT_PUBLIC_META_PIXEL_ID`가 있을 때만 로드한다.
- 이유: 기존 빌드 산출물에서 홈 첫 화면에 폰트 preload 561개, 약 5MB가 잡혔다. 선택 후보 폰트를 모두 미리 받는 것은 체감 속도를 크게 해치므로, 실제 사용 시 필요한 폰트만 브라우저가 요청하게 한다.

## 2026-07-09: 전역 후보 폰트는 next/font/google에서 제거
- 결정: `src/app/layout.tsx`의 전역 `next/font/google` 등록을 제거하고, 관리자 폰트 선택값은 `src/lib/fonts.ts`의 CSS fallback 스택으로 처리한다.
- 이유: `preload: false`는 브라우저 선로딩만 막고, `next/font/google`은 여전히 빌드 시 Google Fonts와 `fonts.gstatic.com` 파일을 다운로드한다. 외부 네트워크 불안정이 빌드 전체를 막지 않게 하려면 후보 폰트를 빌드 단계에서 가져오지 않아야 한다.

## 2026-07-09: 런타임 폰트 stylesheet는 첫 paint 이후 로드
- 결정: Pretendard와 Material Symbols stylesheet를 전역 `<head>`에서 제거하고 `DeferredFontStyles` 클라이언트 컴포넌트가 첫 paint/idle 이후 삽입한다.
- 이유: 전역 stylesheet는 첫 화면 렌더를 막는 리소스가 될 수 있다. 앱의 기본 CSS와 레이아웃을 먼저 보여주고, 선택 폰트와 아이콘 폰트는 화면이 그려진 뒤 붙이면 홈페이지와 관리자 페이지 첫 반응이 가벼워진다.

## 2026-07-09: 첫 화면 공통 아이콘은 폰트 없는 SVG 컴포넌트를 사용
- 결정: 공개 헤더/푸터/테마/챗봇/가이드/후기/인스타 미리보기와 관리자 shell의 단순 아이콘은 `FontFreeIcon`으로 렌더링하고, `DeferredFontStyles`는 실제 `.material-symbols-outlined`가 있는 페이지에서만 Material Symbols stylesheet를 삽입한다.
- 이유: Material Symbols는 라이브러리 JS보다 가볍지만, 첫 화면에서 아이콘 몇 개 때문에 외부 아이콘 폰트 CSS/폰트 요청이 생길 수 있다. 공통 shell 아이콘을 작은 inline SVG로 바꾸면 홈 첫 HTML에서 Material Symbols 흔적이 사라지고, 아이콘 폰트는 필요한 업무 화면에서만 요청된다.

## 2026-07-09: 학원 기본 설정은 서버 캐시와 태그 무효화를 사용한다
- 결정: `getAcademySettings()`는 `unstable_cache`로 5분 캐시하고, 관리자 설정 저장 시 `academy-settings` 태그를 즉시 무효화한다.
- 이유: 학원 설정은 거의 모든 공개 페이지와 일부 서버 작업에서 반복 사용되지만 자주 바뀌지 않는다. 요청마다 DB를 확인하는 대신 캐시를 두면 전체 페이지의 서버 부담이 줄고, 저장 시 태그 무효화로 최신성도 유지할 수 있다.

## 2026-07-11: 콘텐츠성 관리자 API는 서버 캐시만 짧게 사용한다
- 결정: 프로그램/코치/FAQ/연간일정/SMS 템플릿/청구 템플릿 API는 권한 확인 후 60초 `unstable_cache`를 쓰고, 브라우저 `no-store` 흐름은 유지한다.
- 이유: 브라우저 캐시까지 켜면 저장 직후 목록을 다시 불러올 때 예전 응답이 보일 수 있다. 서버 내부 DB 조회만 재사용하면 관리자 데이터 최신성을 해치지 않으면서 반복 DB 왕복을 줄일 수 있다.

## 2026-07-11: 운영 데이터 API도 서버 캐시와 태그 무효화로 묶는다
- 결정: 원생/반/체험/수강신청/대기자/보강 API는 권한 확인 후 30~60초 `unstable_cache`를 쓰고, 관련 저장 액션에서 태그를 즉시 무효화한다.
- 이유: DB 계측상 원생 전체 조회도 DB 내부 실행은 약 3.4ms로 빠르고, 실제 지연은 여러 API의 인증/왕복/응답 직렬화가 겹치는 쪽에 가깝다. 반복 읽기를 서버에서 짧게 재사용하면 최신성을 유지하면서 체감 지연을 줄일 수 있다.

## 2026-07-11: 관리자 공통 셸의 배경 카운트 조회를 제거한다
- 결정: 모든 관리자 화면에서 7초 뒤 자동 호출하던 `/api/admin/trial-count`를 제거하고, 체험 신청 수는 체험 CRM/대시보드 데이터에서 확인하게 둔다.
- 이유: 작은 배지 하나라도 전역 셸에 붙으면 모든 관리자 화면에서 인증/API 왕복이 추가된다. 자주 보는 정보는 해당 업무 화면에서 확인하고, 공통 셸은 가능한 한 가볍게 유지한다.

## 2026-07-09: 공개 페이지 플로팅 도구는 본체를 지연 로드한다
- 결정: 챗봇은 `ChatPanel`을 버튼 클릭 후 로드하고, 입학 가이드 투어는 `GuideTourLazyTrigger`가 버튼만 먼저 보여준 뒤 기존 투어 본체를 필요 시 로드한다.
- 이유: 챗봇 패널과 가이드 투어는 모든 방문자가 쓰는 기능이 아니지만 모든 공개 페이지에 붙어 있다. 첫 화면에는 버튼만 있으면 충분하므로, 큰 상호작용 코드는 클릭/투어 URL/첫 방문 예열 시점으로 미룬다.

## 2026-07-09: 홈 정적 섹션은 서버 컴포넌트로 렌더링한다
- 결정: `LandingPageClient`의 파일 단위 `use client`를 제거하고, 후기 캐러셀처럼 실제 브라우저 상호작용이 필요한 하위 컴포넌트만 Client Component로 유지한다.
- 이유: 홈 히어로, 신뢰 지표, 과정 안내, 갤러리 하이라이트, CTA는 대부분 정적 렌더링으로 충분하다. 전체를 클라이언트로 묶으면 첫 진입 JS가 커지므로, 서버에서 먼저 HTML을 만들고 작은 상호작용 섬만 hydration한다. 빌드 산출물 기준 홈 entry JS는 4개 합계 약 0.25MB로 확인했다.

## 2026-07-09: 공개 갤러리 라이트박스는 클릭 후 로드한다
- 결정: 갤러리 그리드는 서버 렌더링으로 만들고, 클릭 감지용 `GalleryLightboxController`만 초기 로드하며, 전체화면 `GalleryLightboxOverlay`는 동적 로드한다.
- 이유: 방문자는 갤러리 목록을 먼저 훑고 일부만 사진을 크게 본다. 라이트박스 키보드 이벤트, 스크롤 잠금, 큰 미디어 뷰어 코드를 첫 화면에 싣지 않아도 기본 탐색은 가능하다. 빌드 산출물 기준 `/gallery` entry JS는 4개 합계 약 0.25MB이며 라이트박스 본체는 초기 manifest에서 빠진 것을 확인했다.

## 2026-07-09: 관리자 공지 리치 에디터는 모달에서 로드한다
- 결정: `/admin/notices`는 `RichTextEditor`를 정적 import하지 않고, 새 공지/수정 모달이 렌더될 때 `next/dynamic`으로 로드한다.
- 이유: 선생님/관리자가 공지 목록을 확인하거나 소셜 발행을 누를 때는 리치 에디터가 필요 없다. 큰 편집기 번들을 작성/수정 순간으로 미루면 관리자 목록 초기 진입이 가벼워진다. 빌드 산출물 기준 `/admin/notices` entry JS는 5개 합계 약 0.27MB이며 `RichTextEditor`는 초기 manifest에서 빠진 것을 확인했다.

## 2026-07-09: 공개 헤더 인증 확인은 동적 컴포넌트로 분리한다
- 결정: `PublicHeader`는 Supabase 브라우저 클라이언트를 직접 import하지 않고, 계정 버튼과 로그아웃 UI를 `PublicAccountControls`로 분리해 동적 로드한다.
- 이유: 공개 헤더는 모든 공개 페이지의 첫 화면에 붙지만, 로그인 상태 확인은 본문 렌더링보다 덜 급하다. 인증 SDK를 초기 entry JS에서 빼면 첫 화면 다운로드 부담이 크게 줄어든다. 빌드 산출물 기준 홈 entry JS는 4개 265,411 bytes에서 3개 58,339 bytes로 줄었고, entry 파일에 Supabase 문자열이 없는 것을 확인했다.

## 2026-07-09: 공개 공통 아이콘은 Material Symbols를 우선한다
- 결정: 공개 헤더, 테마 토글, 챗봇 버튼의 단순 아이콘은 `lucide-react` import 대신 Material Symbols 텍스트 아이콘으로 렌더링한다.
- 이유: Material Symbols 폰트는 전역에서 이미 사용 중이므로, 첫 화면 공통 컴포넌트가 별도 아이콘 JS를 import할 필요가 없다. 빌드 산출물 기준 홈 entry JS는 58,339 bytes에서 56,093 bytes로 줄었고, entry 파일에 lucide 문자열이 없는 것을 확인했다.

## 2026-07-09: 관리자 shell은 서버 인증 정보를 재사용한다
- 결정: `/admin` 레이아웃의 `requireAdmin()` 결과에서 사용자 이름/이메일을 shell에 전달하고, `AdminShellClient`는 Supabase 브라우저 클라이언트로 사용자 정보를 다시 조회하지 않는다. 로그아웃은 서버 액션으로 처리하고, 알림은 클릭 시 조회하며 체험 카운트 자동 조회는 두지 않는다.
- 이유: 관리자 레이아웃은 렌더링 전에 이미 인증을 통과한다. 같은 정보를 브라우저에서 다시 조회하면 공통 shell JS와 초기 네트워크 부담이 늘어난다. 빌드 산출물 기준 `/admin` entry JS는 2개 합계 약 0.05MB이며 Supabase 문자열이 없는 것을 확인했다.

## 2026-07-09: 업로드 이미지는 고유 URL 전제로 장기 캐시한다
- 결정: 로컬 fallback 업로드 `/uploads/:path*`에는 `Cache-Control: public, max-age=31536000, immutable`을 적용하고, Supabase Storage 업로드에는 `cacheControl: "31536000"`을 지정한다.
- 이유: 현재 업로드 경로는 타임스탬프와 랜덤값을 포함한 파일명으로 저장되어 같은 URL의 이미지 내용이 바뀌지 않는다. 한 번 받은 이미지를 브라우저와 CDN이 오래 재사용하면 홈페이지와 관리자 페이지의 재방문 이미지 로딩이 빨라진다.

## 2026-07-09: 공개 신청 페이지 HTML sanitize는 서버에서 수행한다
- 결정: `/apply`의 체험/수강신청 안내 HTML은 `page.tsx` 서버 컴포넌트에서 `sanitizeHtml()`로 정리한 뒤 `ApplyPageClient`에 전달한다.
- 이유: `sanitize-html`과 그 하위 파서들은 브라우저에서 실행하기엔 큰 도구다. 서버에서 한 번 정리해 안전한 HTML만 내려보내면 보안은 유지하면서 `/apply` client bundle에서 약 248KB짜리 HTML 파서 chunk를 제거할 수 있다.

## 2026-07-09: 최초 관리자 setup은 서버 API에서 처리한다
- 결정: `/setup` client는 Supabase 브라우저 SDK를 직접 import하지 않고, `/api/auth/check-setup` POST가 Auth 사용자 생성, `User` row upsert, 로그인 쿠키 설정을 처리한다.
- 이유: setup은 최초 1회 사용하는 화면이지만 기존 구조는 Supabase 브라우저 SDK 약 200KB를 첫 JS에 포함했다. 서버 API로 옮기면 같은 UX를 유지하면서 `/setup` client bundle을 크게 줄일 수 있다.

## 2026-07-07: `SocialPostDraft`는 raw SQL 보강 테이블로 둔다
- 결정: Prisma schema 마이그레이션 대신 `CREATE TABLE IF NOT EXISTS "SocialPostDraft"` 패턴을 사용한다.
- 이유: 기존 프로젝트가 Supabase PgBouncer 호환을 위해 raw SQL 보강 패턴을 많이 쓰고 있으며, 이번 기능은 독립 초안 테이블이라 점진 도입이 안전하다.

## 2026-07-07: 인스타 자동 게시 기본 모드는 사진 전용
- 결정: 사진 1장은 단일 게시, 사진 2~10장은 캐러셀 게시로 처리하고 영상 자동 게시는 이번 범위에서 제외한다.
- 이유: 사용자가 원하는 기본 흐름은 “사진만 넣으면 자동 생성/업로드”이며, 영상은 인스타 Graph API 조건과 처리 분기가 더 복잡하다.

## 2026-07-07: 로그인은 통합하고 회원가입에서만 계정 유형 선택
- 결정: 로그인 화면은 `STIZ 로그인` 하나로 통합하고, 회원가입 때만 학부모/선생님 유형을 선택한다.
- 이유: 로그인은 신분을 다시 묻는 곳이 아니라 인증하는 곳이다. 계정 유형과 권한은 DB `User.role`을 기준으로 판단하고, 로그인 후 역할별 기본 화면으로 이동시키는 편이 자연스럽다.

## 2026-07-08: 관리자 화면은 서버 레이아웃에서 권한 확인
- 결정: `/admin` 레이아웃은 서버 컴포넌트에서 `requireAdmin()`을 먼저 실행하고, 통과한 경우에만 클라이언트 관리자 shell을 렌더링한다.
- 이유: 미들웨어의 로그인 확인만으로는 일반 로그인 사용자가 관리자 화면을 볼 수 있다. 관리자 데이터 노출을 막으려면 페이지 렌더링 전 DB role 기준 권한 확인이 필요하다.
## 2026-07-15: 월별 청구서 생성과 학부모 알림은 분리한다
- 결정: 관리자 수납 월별 청구서는 먼저 대상/금액을 미리보기로 확인한 뒤 생성하고, 학부모 SMS 발송은 미납 알림 버튼에서 별도로 처리한다.
- 이유: 청구서 생성은 회계 원장에 기록을 만드는 작업이고, 알림 발송은 학부모에게 실제 안내가 나가는 작업이다. 두 작업을 한 버튼에 묶으면 잘못 생성했을 때 문자까지 나가 복구가 어려워진다. 영수증을 발행하기 전에 장바구니를 확인하는 것처럼, 청구 대상 확인 단계를 먼저 둔다.

## 2026-07-15: 결제 운영은 Payment 원장 위에 청구서/거래/웹훅/감사 로그를 분리해 관리한다
- 결정: 기존 Payment는 금액과 상태의 원장으로 유지하고, 납부 링크와 청구서 번호는 PaymentInvoice, 결제 시도와 Toss 주문번호/paymentKey는 PaymentTransaction, 결제사 알림은 PaymentWebhookEvent, 관리자/시스템 변경 이력은 PaymentAuditLog에 저장한다.
- 이유: 수강료 발행, 온라인 납부, 미납 알림, 수동 납부완료 처리는 같은 Payment를 바라보지만 각각 필요한 기록이 다르다. 한 테이블에 모두 섞으면 운영자가 원인을 추적하기 어려우므로, 종이 장부처럼 원장과 영수증/거래내역/변경기록을 나눠 둔다.
- 2026-07-15: 교사용 학생·청구 정보는 화면 진입 시 미리 받지 않고 패널 최초 오픈 때 수업 단위 단일 SQL로 조회한다. 개인정보 결과는 브라우저 전역 캐시에 보관하지 않으며, 동시에 발생한 같은 요청만 병합한다.
## 2026-07-16: 교사용 수업은 단일 진행과 기록 우선 원칙을 적용한다
- 결정: 교사별 진행 중 수업은 하나로 제한하고 DB 잠금과 서버 재검사를 함께 사용한다.
- 결정: 수업 메모는 자동 저장하며 음성 인식·저장·오프라인 상태에서는 종료를 잠시 막는다.
- 이유: 수업 중 중복 클릭이나 통신 장애가 생겨도 출결과 특이사항이 서로 다른 장부로 갈라지거나 유실되지 않아야 한다.
## 2026-07-16: 역할별 업무 홈과 공개 홈페이지를 양방향 연결한다
- 결정: 일반 로그인은 DB 역할별 업무 홈으로 보내고, 권한이 허용된 명시적 딥링크만 우선한다.
- 결정: 교사용 PWA 범위는 `/staff`로 유지하고 프로필 메뉴에서 공개 홈페이지를 연다.
- 결정: 진행 수업에서 외부 화면으로 이동하기 전 메모 저장 완료를 기다리며 실패하면 이동을 차단한다.
- 이유: 반복 업무 속도와 홈페이지 정보 접근성을 함께 확보하면서 수업 기록 유실을 방지하기 위해서다.
## 2026-07-16: 모든 계정 진입점은 DB 역할을 최종 기준으로 사용한다
- 결정: 공개 홈페이지의 계정 버튼과 마이페이지 접근은 인증 ID 우선, 이메일 보조 방식으로 DB 사용자를 확인한다.
- 결정: 미인증 보호 딥링크는 검색조건까지 보존하되 기존 내부 경로 검증을 반드시 통과시킨다.
- 이유: 오래된 토큰 권한과 계정 정보 변경에도 올바른 업무 화면으로 이동하고, 로그인 전 작업 맥락을 잃지 않기 위해서다.
# 2026-07-20 방학특강 운영 결정

- 신청서를 반별 신청 항목으로 분리하여 부분 승인·대기·취소가 가능하게 한다.
- 신청 접수 후 관리자 확인과 청구서 발행을 거쳐 결제 완료 시 최종 확정한다.
- 동일 시즌의 중복 제출은 idempotency key로 방지하고 만석 신청은 자동 대기 처리한다.
- 방학특강 테이블은 Supabase Data API에 공개하지 않고 RLS와 `anon/authenticated` 권한 회수로 서버 Prisma 전용으로 운영한다.

## 2026-07-21 셔틀 위치 수집 1단계

- 지도는 카카오 JavaScript SDK를 지연 로딩하고, 키 누락이나 장애 시 기존 텍스트 입력을 유지한다.
- 학부모가 제출한 지도 핀은 관리자 검증 완료와 구분하며 주소·좌표·선택 방식·정확도·서버 제출 시각을 함께 저장한다.
- 지도 위치는 서버가 인정하는 동의 버전과 명시적 동의가 있을 때만 저장한다.
- 노선 최적화·기사 앱·GPS보다 위치 수집과 관리자 검토 기반을 먼저 안정화한다.

## 2026-07-21 셔틀 노선 운영 모델

- 결정: 방학특강 실제 운행 노선은 홈페이지 안내용 셔틀 정보와 분리해 `ShuttleVehicle`, `ShuttleRoutePlan`, `ShuttleRouteStop`, `ShuttleRoutePassenger`, `ShuttleAuditLog`로 관리한다.
- 이유: 모집 안내에 쓰는 정적인 셔틀 정보와 당일 운행 배정은 변경 주기와 책임이 다르다. 실제 운행은 차량 정원, 정류장 순서, 학생 배정, 확정 이후 수정 이력이 필요하므로 별도 장부처럼 분리해야 운영 사고를 줄일 수 있다.
- 결정: 확정된 노선은 직접 수정하지 않고 보관 또는 새 수정본 생성으로 변경한다.
- 이유: 기사/학부모에게 안내된 노선은 “현재 배포된 운행표” 역할을 하므로, 수정 이력을 남겨야 어느 버전이 실제 운행 기준인지 추적할 수 있다.

## 2026-07-21: 셔틀 탑승 체크는 기존 운행 배정 승객에 상태를 붙인다

- 결정: 실시간 탑승/하차 체크는 별도 운행 로그 테이블을 먼저 만들지 않고 ShuttleRoutePassenger.rideStatus와 갱신자/갱신시각 필드에 저장한다.
- 이유: 현재 필요한 것은 운행 중 학생별 현재 상태 확인이다. 배정 승객 카드에 상태를 붙이면 기사 앱과 관리자 화면이 같은 데이터를 보게 되어 구조가 단순하다. 이력 분석이 필요해지면 기존 ShuttleAuditLog를 기준으로 확장한다.

## 2026-07-26: 방학특강 "기존회원" = 현재 재원 중(ACTIVE)인 원생만

- 결정: `hasMatchingExistingStudent()`는 이름 + 생년월일 + 학부모 연락처가 모두 일치하고, **그 학생에게 `Enrollment.status = 'ACTIVE'` 등록이 1건 이상** 있을 때만 `EXISTING`으로 판정한다.
- 이유: 기존회원 할인은 "지금 학원에 다니는 원생"에게 주는 혜택이다. 기존 판정은 재원 여부를 전혀 보지 않아 휴원·퇴원생과 과거 이력자까지 할인 대상이 됐다. 2026 여름특강 실측에서 신규 19명 중 5명이 거짓 양성이었고 전원 `PAUSED`이거나 등록 이력이 없었다.
- 결정: 휴원(`PAUSED`)과 퇴원(`WITHDRAWN`)은 할인 대상에서 제외한다.
- 이유: 사용자가 "지금 다니는 학생만"을 명시적으로 선택했다. 과거 재원 이력자·형제자매도 같은 이유로 제외된다.
- 결정: 판정은 반 단위가 아니라 **학생 단위로 ACTIVE 1건 이상**을 본다.
- 이유: 반 이동 시 예전 반 등록이 `PAUSED`로 남기 때문에(운영 규칙 2번 소프트 취소) 반 단위로 보면 정상 재원생이 누락된다.
- 참고: 소급 적용이 아니다. 확정된 신청은 `priceSnapshot`에 금액이 저장되어 있어 이 변경의 영향을 받지 않고, 앞으로 들어올 신규 신청에만 적용된다.
- 기각: 생년월일 비교식 변경과 `INNER JOIN "User"` → `LEFT JOIN` 변경은 실측 결과 현행이 정확해 기각했다. 근거는 `errors.md` 참조.

## 2026-07-26: 방학특강 수강은 기존회원 할인 대상이 아니다 (정규반만 인정)

- 결정: `hasMatchingExistingStudent()`의 재원 판정에서 **방학특강 전용 반의 `Enrollment`는 제외**한다. 정규반에 ACTIVE 등록이 있어야만 기존회원으로 본다.
- 이유: 승인 전환(`api/admin/seasonal/route.ts`)이 `SpecialProgramOffering.linkedClassId` 반에 ACTIVE `Enrollment`를 만든다. 이를 그대로 두면 특강만 수강한 학생이 다음 시즌에 자동으로 정규반 원생 할인을 받는다.
- 결정: 제외 조건은 **`Class."dayOfWeek" = 'Seasonal'`과 "특강에 연결됨"을 모두 만족할 때만** 적용한다. 둘 중 하나만으로 제외하지 않는다.
- 이유: `linkedClassId`는 관리자 자유 입력이고 입력 항목 라벨이 **"연결 정규 반 ID"** 다. 즉 정규반을 특강에 연결하는 것이 설계상 허용된다. "연결됨"만으로 제외하면 그 정규반 원생 **전원이 할인을 잃는** 치명적 부작용이 생긴다. 정규반은 항상 실제 요일(Mon~Sun)을 가지므로 `dayOfWeek='Seasonal'`을 함께 요구하면 이 사고가 구조적으로 불가능해진다.
- 실측(2026-07-26): 특강 전용 반 3개는 전부 `dayOfWeek='Seasonal'`이고, 정규반 33개 중 특강에 연결된 것은 0개다. 이 변경으로 판정이 바뀌는 사람은 0명이며 현재 ACTIVE 원생 147명 전원이 정규반 재원으로 유지된다.
- 알려진 잔여 간극: 관리자가 실제 정규반을 특강에 연결하면 그 특강 수강생이 다음 시즌에 할인을 받을 수 있다. 이는 "할인을 더 주는" 안전한 방향이라 감수한다. 반대 방향(정상 원생이 할인을 잃음)은 절대 발생하지 않아야 한다.
- 주의: `Class`에는 정규/특강 구분 컬럼이 없다. `AttendanceClient`의 `kind: "REGULAR" | "SEASONAL"`은 화면 조립용 타입이지 DB 컬럼이 아니다.

## 2026-07-26: 방학특강 형제할인 10% — 확정 정책 8가지

- **① 형제 판정 = 보호자 전화번호 겹침.** 한 학생의 번호 집합은 `신청서 parentPhone ∪ 매칭 원생의 User.phone ∪ 그 학생의 모든 Guardian.phone`(숫자만 정규화)이고, 하나라도 겹치면 형제다. 합집합이어야 하는 이유는 아빠 번호는 신청서에, 엄마 번호는 원생 보호자에 등록된 사례(송서준)가 실재하기 때문이다.
- **② 할인 대상은 "같은 시즌을 같이 신청한 형제"뿐이다.** 형제가 정규반만 다니는 경우는 대상이 아니다. 판정 단위는 원생이 아니라 **시즌 내 신청서**다.
- **③ 형제 전원 각각 10%** (둘째부터가 아니다).
- **④ 기존회원가에서 또 10%** 중복 적용한다. 기존회원 주2회 150,000 → 135,000.
- **⑤ 수강료만 할인하고 셔틀비는 절대 할인하지 않는다.** 최종금액 = 수강료 − 할인 + 셔틀비.
- **⑥ 반올림은 `Math.floor`(원 단위 절사).** JS는 `Math.floor(tuition * 10 / 100)`, SQL은 `(tuition * 10) / 100` 정수 나눗셈으로 계산해 두 결과가 구조적으로 어긋날 수 없게 했다.
- **⑦ 중복 등록 의심 학생은 할인에서 제외한다.** 판정 기준은 **"이름 앞부분 동일 + 생일 차이 2일 이내"를 모두 만족**할 때만이다. 생일만으로 판단하면 진짜 쌍둥이(장승리/장하리, 이윤건/이윤서, 김루나/김루희)가 할인을 잃는다.
- **⑧ 번호가 잘못 등록된 2쌍(이시윤/최현, 남건희/박하진)은 형제가 아니다.** 이름이 아니라 **"문제의 전화번호 + 두 이름"** 조합으로 예외를 둔다(`MISREGISTERED_PHONE_EXCEPTIONS`). 두 사람을 잇는 공유 번호가 **전부** 예외로 설명될 때만 연결을 끊으므로, 같은 번호를 쓰는 진짜 형제가 나중에 생겨도 영향이 없고 원생 번호가 교정되면 규칙은 저절로 무의미해진다.

### 구조 결정

- **결정: 할인은 언제나 `tuitionPriceSnapshot`(할인 전 수강료)에서만 계산한다.** 저장된 `priceSnapshot`을 다시 입력으로 쓰지 않는다.
- **이유:** 재계산이 여러 경로에서 반복적으로 일어난다(신청, 반 재배정, 항목 추가). 결과값을 입력으로 쓰면 실행할 때마다 10%씩 깎여 나간다. 원가 기준 재계산은 몇 번을 돌려도 결과가 같다(멱등). 실측으로 3회 연속 실행 시 2·3회차 갱신 0건을 확인했다.
- **결정: 금액 로직 중복 2벌을 제거하고 `syncSeasonSiblingDiscounts()` 단일 진입점으로 통일했다.** 관리자 라우트에 있던 `defaultSpecialProgramPrice` / `specialProgramShuttleFee` 사본을 지우고 공개 신청과 같은 `resolveOfferingPrice` / `resolveShuttleFee`를 쓴다.
- **이유:** 한쪽만 고치면 **관리자가 반을 재배정하는 순간 할인이 조용히 사라진다.** 금액을 바꾸는 세 경로(공개 신청 / 반 재배정 / 항목 추가) 모두 저장 직후 같은 함수를 부르도록 강제하고 회귀 테스트로 고정했다.
- **결정: 할인 대상 판정은 시즌 전체를 다시 계산한다.** 동생이 나중에 신청하면 먼저 신청한 형·누나도 그때 비로소 대상이 되기 때문이다.
- **결정: 이미 `enrollmentId`·`paymentId`가 붙은 항목은 절대 금액을 바꾸지 않는다.** 청구서가 나간 뒤의 금액은 확정 금액이다.
- **알려진 동작:** 형제 중 한 명이 신청을 취소하면 다음 재계산 때 남은 한 명의 할인이 사라진다. 규칙 ②("같이 신청")의 논리적 귀결이라 그대로 둔다.

### 마이그레이션 방식 결정

- **결정: `prisma migrate dev`를 쓰지 않고, 멱등 마이그레이션 파일 + 직접 DDL 적용을 선택했다.**
- **이유:** `migrate status` 확인 결과 `20260725120000_add_seasonal_enrollment_makeup`, `20260725130000_add_seasonal_enrollment_attendance` 두 건이 **파일에는 있는데 `_prisma_migrations`에는 없는 드리프트** 상태다. 이 상태에서 `migrate dev`를 실행하면 Prisma가 **DB 리셋을 제안**한다(운영 데이터 전멸 위험). 기존 두 마이그레이션도 `IF NOT EXISTS`로 작성된 것이 이 프로젝트의 확립된 관례이므로 같은 방식을 따랐다.
- **주의(후속 과제):** 이 드리프트는 남아 있다. Vercel `migrate deploy`가 세 마이그레이션을 모두 재실행하지만 전부 멱등이라 무해하다. 그래도 `prisma migrate resolve --applied`로 정리해 두는 편이 안전하다.
- **주의:** `SpecialProgramApplicationItem`에는 `CHECK ("priceSnapshot" = "tuitionPriceSnapshot" + "shuttleFeeSnapshot")` 제약이 있었다. 할인 컬럼만 추가하고 이 제약을 그대로 두면 **할인이 들어가는 순간 모든 저장이 실패**한다. 제약을 `= tuition - discount + shuttle`로 교체해야 한다.
# 2026-08-26: 시트·랠리즈·홈페이지 3중 동기화 원장

- 결정: 학부모 요청 원문은 홈페이지 운영 원장에 먼저 저장하고, 학생별 명령으로 나눈 뒤 시트·랠리즈·홈페이지의 반영 상태를 각각 추적한다.
- 이유: 세 시스템 중 일부만 바뀐 상태를 완료로 오인하면 다음 달 청구에서 같은 오류가 반복된다. 택배 배송조회처럼 목적지별 상태를 따로 기록해야 누락 지점을 바로 찾을 수 있다.
- 결정: 외부 반영은 미리보기와 관리자 승인 뒤에만 수행하고, 한 곳이라도 실패하면 `PARTIAL` 또는 `HELD`로 남긴다.
- 결정: 동일 요청은 SHA-256 idempotency key로 중복 저장·중복 실행을 막는다.
- 결정: 신규 운영 테이블은 서버 전용으로 사용하며 RLS를 켜고 `anon`, `authenticated` 직접 권한을 회수한다.
- 결정: 실제 변경 순서는 `미리보기 승인 → 시트 반영 확인 → 랠리즈 반영 확인 → 홈페이지 최종 반영`으로 고정한다. 홈페이지에는 요청·감사 원장을 먼저 만들지만 재원 상태는 마지막에 바꾼다.
- 이유: 시트나 랠리즈가 실패했는데 홈페이지 재원 상태만 먼저 바뀌는 부분 성공을 방지하기 위해서다.

# 2026-08-27: 학부모 자연어 요청은 학부모 확인 후 원장 승인한다

- 결정: 학부모 자연어는 실제 현재 수강과 개설반을 기준으로 복수 명령 초안으로 해석하고, 학부모가 날짜·현재 반·희망 반·셔틀 의도를 직접 수정한 뒤 확정한다.
- 결정: 모호한 값은 추측하지 않고 확인 질문으로 남기며, 서버가 확정 payload를 실제 날짜·현재 등록·개설반·요청 종류 관계로 다시 검증한다.
- 결정: 학부모 확정은 `DRAFT` 접수일 뿐이며 시트·랠리즈·홈페이지 반영은 원장 승인 뒤에만 수행한다. 청구와 알림은 계속 별도 `HELD` 상태로 둔다.
- 이유: 자연어 모델의 정확도만 믿기보다 학부모와 원장이 각각 확인하는 이중 확인 구조가 잘못된 반 변경·셔틀·청구 반영을 구조적으로 줄인다.
- 결정: 랠리즈는 공식 쓰기 API가 확인되기 전까지 로그인된 브라우저의 감독형 처리로 운영한다. 학생 이름만 보지 않고 전화·생년월일·소속 클래스를 대조하고, 상태 변경 후 목록 재조회까지 끝나야 성공으로 기록한다.
# 2026-08-26: 정규 차량표는 월별 스냅샷으로 보존

- 결정: 정규 차량 시트 가져오기는 전체 교체 대신 적용 월만 교체한다.
- 이유: 전월 대비 차량 시간·정류장 변동을 재현하고, 이전 달 배차와 안내 근거를 감사할 수 있어야 한다.
- 안전장치: 홈페이지에서 연락처로 한 명을 정확히 찾은 경우에만 휴원·퇴원을 제외한다. 불일치·동명이인은 `확인보류`로 유지한다.
- 알림: 비교 화면은 정확한 문자 초안까지만 만들며 발송 API를 호출하지 않는다. 별도 미리보기 승인 전 발송은 금지한다.
# 2026-08-27: 예약 대조와 별도로 수강 변경을 이벤트 원장에 즉시 적재

- 결정: 학생 등록·활성·휴원·퇴원·반 변경이 사이트에서 확정되면 기존 운영 동기화 원장에 같은 트랜잭션으로 즉시 기록한다. 신규 평행 장부는 만들지 않는다.
- 이유: 매일 오전 10시 대조만 사용하면 최대 하루 동안 시트·랠리즈·사이트가 서로 다른 상태다. 기존 원장은 대상별 상태, 임대 토큰, 멱등 키, 감사 로그를 이미 갖고 있어 실시간 큐의 기반으로 재사용하는 편이 가장 단순하다.
- 결정: 출처 시스템의 시도는 `SUCCEEDED`로 시작하고 나머지 시스템만 `PENDING`으로 둔다. 모든 흐름을 `SHEET → RALLYZ → WEBSITE` 고정 순서로 강제하지 않고, `sourceSystem`에 따라 남은 목적지를 계산한다.
- 이유: 사이트에서 이미 확정된 변경을 마지막 단계에서 다시 사이트에 쓰면 충돌과 중복이 생긴다. 출발역은 통과 처리하고 남은 역만 방문해야 한다.
- 결정: 이번 단계는 변경 즉시 원장 적재까지 구현한다. 원장 소비 워커와 매분 재시도는 다음 단계로 두고, 오전 10시 전체 대조는 계속 유지한다.
- 이유: 외부 API 장애나 서버 종료가 있어도 우선 변경 사실을 잃지 않는 것이 첫 단계다. 워커가 완성되기 전에는 `PENDING`을 성공으로 표시하지 않는다.
- 결정: Rallyz 공식 API·웹훅이 없는 동안은 로그인 브라우저 기반 감독형 반영을 유지한다. 연결 부재를 성공으로 간주하지 않는다.
- 결정: 실시간 동기화는 학생·수강·셔틀·출결의 정합 작업만 다루고, 청구·결제·환불·외부 알림은 별도 승인 상태를 유지한다.

# 2026-08-28: 관리자 SSR에서는 운영 DB 구조를 변경하지 않는다

- 결정: 운영 동기화 테이블 생성·보강·RLS·권한 설정은 순차 migration으로만 수행하고 관리자 화면 조회는 구조 확인만 한다.
- 이유: 화면 요청은 손님이 문을 여는 과정과 같아, 그 순간 건물 공사까지 하면 병렬 요청끼리 충돌하고 권한 차이로 간헐 장애가 난다.
- 안전장치: 런타임과 배포 검사가 동일한 7개 테이블·85개 컬럼 계약을 확인하고, 배포 검사는 멱등 고유키·RLS·직접 권한도 함께 확인한다.

# 2026-08-28: 혼합형 3중 동기화 화면은 폐기하고 원장은 유지한다

- 결정: 관리자 메뉴와 `/admin/operations-sync` 화면은 폐기하되 기존 주소는 관리자 홈으로 안전하게 이동한다.
- 이유: 링크 생성, 직접 입력, Rallyz 출석, 승인 원장이 한 화면에 섞여 실제 운영 순서가 불명확하고 모바일에서 사용할 수 없었다.
- 결정: 공개 요청, 동기화 원장, 출석·청구 보류 백엔드는 유지한다. 기존 링크의 생성·취소는 학생 상세로 옮기고 동명이인은 이름이 아닌 `studentId`로 구분한다.
- 결정: 카카오채널은 향후 접근 창구로 사용하되 원장 승인 전에는 어떤 시스템도 변경하지 않는다.

# 2026-08-30: 수강 승인과 외부 알림은 분리한다

- 결정: 수강신청 승인은 기본적으로 문자·푸시·관리자 알림을 보내지 않는다. 승인 화면에는 단순 알림 체크박스를 두지 않는다.
- 결정: 신규·복귀 수강은 Enrollment 변경과 Operations 원장 적재를 같은 트랜잭션으로 처리하고, 이미 ACTIVE인 수강은 완전한 no-op으로 둔다.
- 이유: 수신자·마스킹 연락처·문구·건수를 보여주지 않는 체크박스는 발송 직전 승인으로 충분하지 않다. 알림은 별도의 정확한 미리보기에서만 승인해야 한다.

# 2026-08-30: 신규 학생 최초 등록은 청구와 Rallyz 초대까지 완료한다

- 결정: 실제 신규 학생은 시트·Rallyz·사이트의 학생/수강 등록, 최초 청구서와 청구 알림, Rallyz 학부모 초대장까지 완료해야 등록 완료로 본다.
- 결정: 월 중간 시작은 공식 일정의 남은 확정 회차로 일할 계산하고, 셔틀은 신청 및 운행 가능 요일일 때만 비용·배차에 포함한다.
- 안전장치: 청구·알림·초대는 필수 후속 단계지만 자동 승인으로 보지 않는다. 정확한 금액·기간·마스킹 수신자·문구·건수 미리보기를 실행 직전에 승인받고, 실행 후 세 시스템과 청구·연결 상태를 재조회한다.
# 카카오 학부모 인증과 접수 원장 (2026-08-31)

- 카카오 채널은 홈페이지 공개 링크가 아니라 기존 학부모 요청 접수 창구로 사용한다.
- 챗봇 `botUserKey/plusfriendUserKey` 원문은 저장하지 않고 서버 비밀키 HMAC 해시로만 식별한다.
- 최초 1회는 15분 만료 일회용 링크와 검증된 학부모 로그인으로 연결한다. 이후 학생은 `Student.parentId`로만 찾고 이름 단독 매칭은 하지 않는다.
- 자유문장은 21종 요청으로 분류하지만 곧바로 외부 시스템을 변경하지 않는다. 학부모 요약 확인 뒤 `KakaoParentIntake`에 접수하며 수강·정규 셔틀·청구 변경은 관리자 승인 대상으로 유지한다.
- 카카오 챗봇 스킬 요청은 관리자센터에 저장한 별도 헤더 비밀키가 맞을 때만 받는다.
# 2026-08-31 관리자 대량 문자 발송은 접수와 전송을 분리한다

- 메시지센터의 대량 문자는 최대 500명을 한 배치로 먼저 장부에 예약하고 즉시 배치 ID를 반환한다.
- 기존 신청·체험·셔틀의 단건 `sendManualSms` 계약은 유지한다.
- 예약 워커는 1분마다 최대 5건을 `FOR UPDATE SKIP LOCKED`로 선점한다. 동일 requestId는 advisory lock과 수신자 집합·본문 지문으로 같은 배치를 재사용한다.
- 공급자 도달 여부가 불명확한 `SENDING`은 자동 재발송하지 않고 `UNCERTAIN`으로 격리한다.
- 대기 중 전화번호와 본문은 AES-256-GCM으로 암호화하고, 성공·실패·격리 확정 시 payload를 삭제한다. 관리자 상태 API에는 끝 4자리만 제공한다.

# 2026-09-02 유니폼 주문 항목은 발주 옵션을 개별 필드로 저장한다

- 결정: 디자인과 이니셜을 `UniformOrderItem.design`, `UniformOrderItem.initials`로 저장하고, 본사 payload 옵션에도 같은 필드를 포함한다.
- 이유: 디자인·이니셜이 메모에 섞이면 주문 재전송, 중복 판정, 본사 발주 대조 때 사람이 다시 해석해야 한다. 옷 주문서의 칸을 따로 만드는 것처럼 필드를 나눠야 자동화가 안전하다.
- 결정: 운영 DB 기존 주문 2건은 원본 시트 값 기준으로 디자인·이니셜을 보강하되, 본사 주문 전송과 SMS 발송은 하지 않는다.
- 결정: HMAC 비밀값은 로컬 `.env.local`에만 설정하고 문서·커밋·로그에 남기지 않는다. 본사 서버와 운영 배포 환경에는 같은 값을 별도로 설정해야 한다.

# 2026-09-02 토스 온라인 PG는 청구서 장부를 기준으로 확정한다

- 결정: 학부모 결제는 `PaymentInvoice` 링크에서 시작하고, 토스 성공 복귀 뒤 서버가 `paymentKey`, `orderId`, `invoiceId`, 금액, 보호자 인증 ID를 모두 대조한 뒤 `Payment`, `PaymentInvoice`, `PaymentTransaction`을 한 번에 납부 완료 처리한다.
- 이유: 결제창 복귀 URL은 사용자가 보는 입구일 뿐이다. 실제 납부 확정은 은행 장부를 대조하듯 서버에서 토스 승인 결과와 우리 청구 장부가 맞는지 확인해야 안전하다.
- 결정: 결제 소유권은 이메일 fallback 없이 Supabase Auth ID와 내부 `User.authUserId`만 허용한다.
- 결정: 토스 승인 API가 5xx, 429, 네트워크 오류처럼 불명확하면 즉시 실패 처리하지 않고 재확인 가능한 `retryable` 상태로 남긴다.
- 결정: 토스 웹훅에 이벤트 ID가 없으면 결제키·주문번호·상태·금액으로 서버 생성 eventId를 만들어 중복 처리를 막는다.

# 2026-09-02 정규 셔틀은 수업시간별 차량 실행을 한 방향 저장본에 함께 보존한다

- 결정: 등원은 학생의 수업 시작시각, 하원은 종료시각이 같은 학생끼리 별도의 차량 실행으로 자동 배차한다.
- 이유: 요일 전체의 가장 이른 시작과 가장 늦은 종료만 사용하면 서로 다른 수업 학생이 한 번의 운행으로 합쳐져 실제 등하원 시간이 틀어진다.
- 결정: DB 행을 시간대마다 늘리지 않고 기존 월·요일·방향 저장본의 `vehicles` 배열에 실행별 `classStart`, `classEnd`, `timeGroupLabel`을 함께 저장한다.
- 이유: 화면도 한 방향의 모든 실행을 한 번에 편집·저장하므로 JSON 전체를 단일 승인 단위로 유지하면 시간대별 저장 덮어쓰기 없이 기존 기사·학부모 ETA 조회와 호환된다.
