import assert from"node:assert/strict";import test from"node:test";import{readFileSync}from"node:fs";const source=readFileSync(new URL("../src/lib/dataRetention.ts",import.meta.url),"utf8");test("완료된 단기 운영 로그만 기간 후 정리한다",()=>{assert.match(source,/NotificationDelivery[^`]+180 days/s);assert.match(source,/status='DELETED'[^`]+90 days/s)});test("금융 감사와 사진 회수 증거는 자동 삭제하지 않는다",()=>{assert.doesNotMatch(source,/PaymentAuditLog|PaymentTransaction|MediaRevocationJob|MANUAL_REQUIRED/) });
